# 매주 월요일 08:00 (한국시간). 이 서버의 시스템 시간대는 America/New_York
# 이므로 OnCalendar에 시간대를 반드시 명시한다(systemd 252+ 지원).
[Unit]
Description=Vieworks menu bot weekly collect timer (월 08:00 KST)

[Timer]
OnCalendar=Mon 08:00 Asia/Seoul
Persistent=true
Unit=menubot-collect.service

[Install]
WantedBy=timers.target
