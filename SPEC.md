# 뷰밥 메뉴 알리미 사양서

<!-- spec-template: v1 -->

| 항목 | 값 |
|---|---|
| Document Version | 1.1.0 |
| Project Version | 0.1.0 |
| Last Updated | 2026-09-25 |
| Status | active |
| Owner | hongmin |

이 문서는 `cafeteria-menu-kakao-bot`가 **어떻게 동작해야 하는가**를 정한 기준이다.
코드가 지금 그렇게 동작한다고 해서 그것이 사양이 되지는 않는다.

사양과 구현이 다르면 코드에 맞춰 이 문서를 고치지 않는다. `SPEC / CODE MISMATCH`로 알린다.
그 절차는 `AGENTS.md`에 있다.

프로젝트 규모에 따라 아래 절은 줄일 수 있다. 다만 유지보수형 자동화라면
**1 목적 · 2 범위 · 5 기능 요구사항 · 9 오류 처리 · 11 테스트 사양 · 12 추적성**은 반드시 채운다.

## 1. 목적

사내 구성원이 카카오톡에서 "금요일 점심"처럼 평소 말투로 물으면, 그날 그 끼니의 메뉴를 알려준다.
주간 식단표는 그룹웨어 자유게시판에 이미지로만 올라온다. 이 봇이 그 이미지를 대신 읽는다.

목적은 사람이 게시판을 찾아 들어가 이미지를 확대해 읽는 수고를 없애는 것이다.

성공 조건:

- 근무일(월~금) 업무 시작 시각에 그 주 식단이 DB에 들어와 있다.
- 카카오 질문에는 **실제 날짜가 적힌** 메뉴 응답이 돌아간다. 답할 수 없으면 까닭이 분명한 안내가 돌아간다.

### 이 문서에서 쓰는 말

아래 말에는 `docs/SPEC.html`에서 마우스를 올리면 뜻이 보인다.

| 용어 | 뜻 |
|---|---|
| 그룹웨어 | 식단 게시물이 올라오는 사내 업무 시스템. 로그인해야 읽을 수 있다 |
| 식단표 | 한 주의 날짜·끼니·코너별 메뉴를 담은 표 이미지. 자유게시판 게시물 본문에 올라온다 |
| 끼니 | 조식·중식·석식 가운데 하나 |
| 코너 | 한 끼니 안의 메뉴 묶음. 일반식·PLUS 코너·간편식·건강식이 있다 |
| 현재 주(이번 주) | 오늘이 속한 주의 월요일부터 일요일까지. 사용자에게는 이 주의 식단만 보여준다 |
| 대상 주차 | 수집하거나 확인하려는 주. 그 주의 월요일로 가리킨다 |
| OCR | 이미지 속 글자를 읽어 문자열로 바꾸는 일 |
| OCR provider | 글자 인식을 맡는 프로그램. macOS는 Apple Vision, Windows·Linux는 PaddleOCR을 쓴다 |
| OCR 라인 | OCR이 읽은 글자 한 줄. 글자, 신뢰도, 이미지 안의 위치(x·y·width·height)를 담는다 |
| OCR 캐시 | 이미지 옆에 저장해 둔 OCR 결과 JSON 파일. 있으면 다시 인식하지 않는다 |
| 파서 | OCR 라인을 날짜·끼니·코너 칸에 나눠 넣는 부분 |
| 어휘집(`MENU_VOCAB_PATH`) | 메뉴 이름과 등장 횟수를 담은 JSON 파일. 오인식 교정에 쓰고, 저장소에 넣지 않는다 |
| 운영 DB(`DATABASE_PATH`) | 사용자 질문에 답할 때 쓰는 SQLite 파일. 현재 주 식단만 둔다 |
| 학습용 DB(`--learning`) | OCR·파서 검증용 1년치 과거 데이터를 담는 별도 파일. 운영 응답에 쓰지 않는다 |
| 게시물 번호(`source_post_id`) | 식단 게시물마다 붙은 번호. 저장은 이 번호 단위로 바꿔 넣는다 |
| 확보 | 대상 주차의 메뉴 항목이 1건 이상 저장된 상태 |
| 주말 확인(주말 폴링) | 주말에 정해진 간격으로 게시물 제목을 읽어 다음 주 식단표가 올라왔는지 보는 일 |
| 상태 파일(`NEXT_WEEK_STATE_PATH`) | 주말 확인이 대상 주차·확보 여부·시도 기록을 적어 두는 JSON 파일 |
| 미운영(`no_service`) | 휴무 등으로 식당이 그 끼니를 운영하지 않는 상태 |
| 특식(`special`) | 특식·DAY·영양사 픽 같은 문구가 든 칸의 상태 |
| 되묻기 | 답할 수 없는 질문에 메뉴 대신 무엇이 문제인지 알려 주는 응답 |
| 발화 | 사용자가 카카오톡으로 보낸 말 |
| 카카오 Skill | 카카오 챗봇이 발화를 보내고 답을 받아 가는 서버 연결 방식. 답은 SkillResponse 2.0 형식이다 |
| 말풍선 | 카카오톡에 보이는 답 한 덩어리. 한 응답에 3개까지 보낼 수 있다 |
| 웹훅 토큰(`KAKAO_WEBHOOK_TOKEN`) | 챗봇 서버 주소 경로에 붙이는 긴 임의 문자열. 맞지 않는 요청은 404로 답한다 |
| 루프백 | 같은 컴퓨터 안에서만 닿는 주소 |
| 터널 | 루프백에만 열린 서버를 바깥에서 부를 수 있게 이어 주는 연결. 고정 도메인은 `NGROK_DOMAIN`으로 정한다 |
| 상시 유닛·타이머 | 서버에서 항상 켜 두는 서비스(웹 서버·터널)와, 정해진 시각에 작업을 돌리는 실행 예약 |

## 2. 프로젝트 범위

### 포함

- 그룹웨어 자유게시판에서 주간 식단 게시물과 본문 이미지를 모은다 (Playwright).
- 플랫폼에 맞는 OCR provider로 이미지 글자를 읽는다. macOS는 Apple Vision, Windows·Linux는 PaddleOCR이다.
- 표 안 글자의 위치로 날짜 · 끼니(조식·중식·석식) · 코너(일반식·PLUS 코너·간편식·건강식)를 나눈다.
- OCR이 잘못 읽은 글자와 잡음 조각을 바로잡는다.
- SQLite에 **현재 주** 식단을 저장한다.
- FastAPI로 만든 카카오 Skill API가 평소 말투의 질문을 해석해 답한다.
- 주말 확인으로 다음 주 식단표를 미리 받아 둔다. 받지 못하면 알림 메일을 보내고 시간마다 다시 시도한다.

### 제외

- **과거 주·다음 주 식단 조회.** 사용자에게는 현재 주만 보여준다. 1년치 과거 데이터는
  OCR·파서 검증(`--learning` 모드)에만 쓰고, 운영 DB와 같은 파일을 쓰지 않는다.
- **주말 식단.** 주말에는 식당을 운영하지 않으므로 안내 문구만 돌려준다.
- **사업장마다 다른 식단 응답.** 공통 식단만 보여주고, 응답에 지점 이름을 드러내지 않는다.
- **없는 정보를 추측하거나 지어내기.** 식단표가 아직 없으면 지어내지 않고 사정을 그대로 알린다.
- 카카오 유료 Event API(먼저 보내는 푸시 알림), 사용자 인증·개인화, 식단 예약·주문 기능.

## 3. 시스템 구성

| 구성 요소 | 파일 | 책임 |
|---|---|---|
| 수집기 | `src/menu_bot/scraper.py` | 그룹웨어 로그인, 게시물 제목·본문 이미지 URL 수집 |
| OCR | `src/menu_bot/ocr.py`, `src/menu_bot/ocr_provider.py`, `src/menu_bot/vision_ocr.swift` | 플랫폼에 맞는 provider 선택, 같은 좌표 규칙으로 맞추기, 결과 캐시 |
| 파서 | `src/menu_bot/parser.py` | OCR 라인을 날짜·끼니·코너 칸에 넣기, 휴무·특식 판정 |
| 교정 | `src/menu_bot/corrections.py` | 잡음 제거, 고정 표기 교정, 어휘집 교정 |
| 파이프라인 | `src/menu_bot/pipeline.py` | 이미지 내려받기, 중복 제거, 주차 거르기, 저장 |
| 저장소 | `src/menu_bot/db.py`, `src/menu_bot/models.py` | SQLite 열 구성과 조회 |
| 질의 | `src/menu_bot/query.py` | 질문을 표준 표현으로 바꾸기·해석·되묻기, 응답 문구 만들기 |
| API | `src/menu_bot/web.py` | 카카오 Skill 주소, 토큰 검사, 말풍선 구성 |
| CLI | `src/menu_bot/cli.py` | `scrape` · `ingest` · `ask` · `check-next-week` · `next-week-deadline` · `ensure-menu` · `notify-test` · `build-vocab` |
| 주말 감시 | `src/menu_bot/next_week_watch.py`, `src/menu_bot/notify.py` | 다음 주 확보 판정, 상태 파일, 알림 메일 |
| 설정 | `src/menu_bot/config.py`, `.env.example` | 환경변수 읽기 |
| 운영 | `scripts/systemd/`, `scripts/register-linux-services.sh` | 서비스·타이머 등록 |

바깥 시스템과 닿는 곳은 세 군데다.

- 그룹웨어: 읽기만 한다. 로그인이 필요하다.
- 카카오 챗봇 Skill 서버: 요청을 받는다.
- SMTP 서버: 알림 메일을 보낸다.

이 세 곳 말고는 이미지나 인식 결과를 밖으로 내보내지 않는다.

## 4. 전체 동작 흐름

수집은 세 가지 때에 돈다. 월요일 08:00 정기 수집, 주말 확인(폴링), 시간마다 하는 재시도다.

1. 그룹웨어에 로그인하고 자유게시판 목록을 연다.
2. 제목이 `POST_PREFIXES`로 시작하는 게시물만 고른다.
3. 게시물 본문에서 식단표로 볼 수 있는 큰 이미지의 URL을 모은다.
4. 이미지를 내려받아 OCR한다. 결과는 이미지 옆 OCR 결과 JSON 파일에 저장해 두고 다시 쓴다(캐시).
5. OCR 라인의 위치(좌표)로 날짜 열 · 끼니 행 · 코너 행을 찾고, 칸마다 메뉴 항목을 넣는다.
6. 항목을 바로잡고 중복을 없앤다. 운영 수집이면 대상 주차(월~일) 밖의 항목을 버린다.
7. 게시물 단위로 SQLite에 덮어쓴다. 같은 `source_post_id`는 항상 새것으로 바꾼다.

응답:

1. 카카오가 발화를 담은 Skill 요청을 보낸다.
2. 토큰을 검사한다. 도움말 요청과 식단 질문이 아닌 발화를 먼저 걸러낸다.
3. 발화를 표준 표현으로 바꾼 뒤 날짜·요일·끼니·코너를 읽는다. 애매하거나 지원하지 않는 요청이면 되묻는다.
4. 현재 주 평일이면 DB를 조회해 끼니 → 코너 → 항목 순의 세로 목록을 만든다.
5. 끼니별 말풍선으로 나눠 SkillResponse 2.0 형식으로 돌려준다.

## 5. 기능 요구사항

ID 규칙: `REQ-<CATEGORY>-NNN`. CATEGORY는 대문자·숫자, NNN은 세 자리다.
한 번 붙인 ID는 다시 쓰거나 뜻을 바꾸지 않는다. 지운 ID를 다른 기능에 돌려쓰지 않는다.

기능 그룹: CATEGORY마다 사람이 읽는 이름이다. `docs/SPEC.html`의 기능 목록이 이 이름으로 묶인다.

| 카테고리 | 이름 |
|---|---|
| SCRAPE | 게시판 수집 |
| OCR | 이미지 인식 |
| PARSE | 식단표 해석 |
| CORRECT | 메뉴 보정 |
| STORE | 저장 |
| QUERY | 질문 해석·응답 |
| API | 챗봇 서버 |
| WATCH | 다음 주 식단 감시 |
| SEC | 보안 |
| OPS | 운영 |
| PORT | 이식성 |

### REQ-SCRAPE-001 게시판에서 식단표 이미지 모으기

#### 목적
그룹웨어 자유게시판에서 식단 게시물과, 그 본문에 든 식단표 이미지의 URL을 얻는다.

#### 입력
`GROUPWARE_URL`, `GROUPWARE_USER`, `GROUPWARE_PASSWORD`, `POST_PREFIXES`, `BROWSER_CHANNEL`,
조회할 목록 페이지 수.

#### 선행 조건
계정 정보가 설정돼 있고, 네트워크로 그룹웨어에 접속할 수 있다.

#### 동작
1. 화면 없이 도는 브라우저(headless)로 로그인한 뒤 자유게시판 목록을 연다.
2. 제목이 `[<사업장>]` 형태이고 `POST_PREFIXES` 중 하나로 시작하는 게시물만 고른다.
3. 고른 게시물을 열어 본문의 큰 이미지만 식단표 후보로 모은다.
   큰 이미지는 가로·세로가 500px보다 크고, 게시판 첨부 이미지 URL 형식을 따르는 이미지다.

제목만 있으면 되는 경우(주말 확인)에는 게시물 본문을 열지 않는 가벼운 목록 조회를 쓴다.

#### 기대 결과
게시물 번호 · 제목 · 이미지 URL 목록을 담은 항목들의 목록.

#### 예외 처리
- 계정 정보가 비어 있으면 그룹웨어에 접속하지 않고 바로 멈춘다.
- 로그인 화면은 여러 번 다른 화면으로 넘어가며(리다이렉트) 페이지 이동 중단 오류(navigation)를 낸다.
  결국 마지막 화면에 도착하므로 이 오류는 무시하고, 화면이 자리 잡을 시간을 더 준다.
  그 밖의 브라우저 오류는 그대로 올린다.
- 브라우저는 플랫폼에 맞게 고른다. Chrome이 없는 Linux 서버에서는 함께 설치된 Chromium을 쓰고 샌드박스를 끈다.
  이유: 권한 없는 사용자용 격리 공간(비특권 user namespace)이 막힌 환경이 있다.

#### 관련 구현
`src/menu_bot/scraper.py`, `src/menu_bot/config.py`

#### 관련 테스트
TEST-SCRAPE-001 (수동)

### REQ-OCR-001 OCR 결과의 공통 좌표 규칙

#### 목적
어느 플랫폼에서든 파서가 같은 좌표 규칙으로 OCR 결과를 받는다.

#### 입력
이미지 파일 경로, `OCR_PROVIDER`(`auto` | `apple_vision` | `paddleocr`).

#### 선행 조건
고른 provider를 돌릴 환경이 갖춰져 있다. macOS는 Swift 툴체인, Windows·Linux는 PaddleOCR이 필요하다.

#### 동작
1. `auto`는 macOS에서 Apple Vision을, 그 밖에서는 PaddleOCR을 고른다.
2. 두 provider 모두 `{"text", "confidence", "x", "y", "width", "height"}` 형태의 라인 목록을 돌려준다.
3. 좌표는 0~1 사이의 비율로 바꿔 적는다. 기준점은 **이미지 왼쪽 아래**이고, y는 위로 갈수록 커진다.
4. 왼쪽 위 기준의 픽셀 좌표를 쓰는 provider는 이 규칙으로 바꿔서 내보낸다.
5. 인식 결과는 이미지 옆 OCR 결과 JSON 파일에 저장해 둔다(캐시). 캐시가 있으면 다시 인식하지 않는다.

#### 기대 결과
같은 이미지라면 provider가 달라도 파서가 같은 칸 구성과 같은 휴무/특식 판정을 얻는다.

#### 예외 처리
- 알 수 없는 `OCR_PROVIDER` 값이면 오류로 멈춘다.
- PaddleOCR 모델 이름과 가속 옵션은 코드를 고치지 않고 환경변수로 바꿀 수 있어야 한다.
  이유: 장비마다 되는 조합이 다르다.
- 값만 비운 환경변수는 "지정하지 않음"으로 보고 기본값을 쓴다.
- 글자 위치를 찾는 감지 모델과 글자를 읽는 인식 모델의 이름을 둘 다 적는다.
  이유: 하나만 지정하면 한국어 인식 모델이 모르는 사이에 바뀐다.

#### 관련 구현
`src/menu_bot/ocr.py`, `src/menu_bot/ocr_provider.py`, `src/menu_bot/vision_ocr.swift`

#### 관련 테스트
TEST-OCR-001 (수동)

### REQ-PARSE-001 식단표를 날짜·끼니·코너로 나누기

#### 목적
식단표 이미지의 OCR 라인을 날짜 · 끼니 · 코너별로 나눈다.

#### 입력
게시물 정보(사업장, 주 시작일), 이미지 URL, OCR 라인 목록.

#### 선행 조건
OCR 라인이 REQ-OCR-001의 좌표 규칙을 따른다.

#### 동작
1. 표 위쪽의 날짜 머리글로 날짜 열을 찾는다.
2. 표 왼쪽의 끼니·코너 이름표로 행을 찾는다.
3. 메뉴 라인마다 가장 가까운 날짜 열과, 자신이 속한 끼니·코너 행에 넣는다.
4. 게시물 제목에서 사업장과 주 시작일을 읽는다. 두 자리 날짜 표기는 주 시작일에 가장 가까운 해로 본다.

끼니 행의 경계는 각 끼니가 `일반식`으로 시작한다는 표 구조로 정한다. 코너 행을 끼니에 묶고, 그 경계로 나눈다.
이유: 끼니 이름표는 세로로 합친 칸의 가운데에 있다. 가장 가까운 이름표에 넣으면 틀린다.

#### 기대 결과
`(날짜, 사업장, 끼니, 코너)`마다 메뉴 항목 문자열 하나.

#### 예외 처리
- 제목에서 사업장이나 날짜를 읽지 못하면 그 게시물은 오류로 기록하고 건너뛴다.
- 날짜 머리글이나 끼니 이름표가 없는 이미지(안내 포스터 등)는 식단표가 아니다.
  0건을 돌려주고, 파이프라인이 "제외한 이미지"로 센다.
- 주 시작일에서 10일 넘게 떨어진 날짜, 표 바깥 위치의 라인, 로고·장식 문구는 버린다.
- 같은 칸이 여러 이미지에서 겹치면 더 긴 결과를 쓴다. 없는 코너를 만들지 않는다.

#### 관련 구현
`src/menu_bot/parser.py`, `src/menu_bot/pipeline.py`, `src/menu_bot/models.py`

#### 관련 테스트
TEST-PARSE-001

### REQ-PARSE-002 미운영·특식 상태 구분

#### 목적
미운영과 특식은 메뉴 항목으로 넣지 않고, 항목의 상태로 구분한다.

#### 입력
REQ-PARSE-001이 칸에 넣은 글자.

#### 선행 조건
없음.

#### 동작
- 대체휴무 · 공휴일 · 전사휴무처럼 **하루 전체**를 뜻하는 문구는 그날 조식·중식·석식
  세 끼 모두에 미운영 항목으로 넣는다. 코너는 `안내`다.
- `조식 미제공`, `석식 운영 없음`처럼 끼니를 밝힌 문구는 **그 끼니에만** 넣는다.
- 특식 · DAY · 영양사 픽 같은 문구가 든 칸은 특식 상태로 표시한다.
- 그 밖은 일반 상태다.

#### 기대 결과
항목마다 상태가 `normal`(일반) · `no_service`(미운영) · `special`(특식) 중 하나다.
응답에서 미운영은 안내 문구로, 특식은 따로 표시해 구분한다.

#### 예외 처리
- 휴무 안내 그림 안의 큰 숫자를 날짜 머리글로 잘못 읽지 않는다.
- 특식 때문에 비어 있는 코너를 추측으로 채우지 않는다.

#### 관련 구현
`src/menu_bot/parser.py`

#### 관련 테스트
TEST-PARSE-002

### REQ-CORRECT-001 메뉴 이름 오인식 바로잡기

#### 목적
사용자에게 그대로 보여줄 메뉴 이름에서, OCR이 잘못 읽은 글자와 잡음 조각을 걷어낸다.

#### 입력
칸에서 모은 항목 문자열 목록, 있으면 쓰는 어휘집(`MENU_VOCAB_PATH`).

#### 선행 조건
없음. 어휘집이 없어도 동작해야 한다.

#### 동작
세 단계로 처리한다.

1. **잡음 제거**: 한글이 한 글자도 없는 항목은 버린다.
2. **고정 표기 교정**: 한국어에 없는 표기를 고정 대응표로 바꾼다.
   - 대응표에는 실제 이미지로 두 provider 결과를 맞대어 확인한 것만 넣는다.
   - 빈도가 비슷해서 원래 표기일 수도 있는 차이는 손대지 않는다.
3. **어휘집 교정**: 어휘집에 없는 항목을 어휘집 항목으로 바꾼다. 아래 조건을 모두 채울 때만 바꾼다.
   - 길이가 같고 한 글자만 다르다.
   - 그런 후보가 **딱 하나**다.
   - 후보는 일정 횟수 이상 나온 어휘집 항목이다. 이유: 어휘집 자체에도 오인식이 섞여 있다.

교정한 뒤 중복은 순서를 그대로 둔 채 없앤다. 어휘집을 새로 만들 때는 어휘집 교정을 끈다.

#### 기대 결과
교정 규칙이 노린 항목만 바꾼다. 규칙과 글자가 겹치는 진짜 메뉴 이름은 망가뜨리지 않는다.

#### 예외 처리
- 어휘집 파일이 없거나, 깨졌거나, 예전 형식(목록)이어도 오류로 보지 않는다.
- 어휘집은 실제 메뉴 데이터이므로 저장소에 넣지 않는다.
- 원본 OCR 결과는 캐시에 남아 있다. 그래서 규칙을 고친 뒤 다시 만들 수 있다.

#### 관련 구현
`src/menu_bot/corrections.py`, `src/menu_bot/cli.py`

#### 관련 테스트
TEST-CORRECT-001, TEST-PARSE-001

### REQ-STORE-001 현재 주 식단 저장

#### 목적
운영 DB에는 현재 주 식단만 둔다. 검증용 과거 데이터는 다른 파일에 둔다.

#### 입력
파서 결과, 대상 주차의 월요일, `DATABASE_PATH`, `--learning` 여부.

#### 선행 조건
없음.

#### 동작
1. 운영 수집은 저장하기 전에 대상 주차 월~일 밖의 항목을 버리고, 버린 개수를 통계로 낸다.
2. 저장은 게시물 단위로 바꿔 넣는다. 같은 `source_post_id`의 기존 항목을 지우고 새로 넣는다.
   그래서 같은 게시물을 여러 번 처리해도 결과가 같다.
3. `--learning`은 모든 날짜를 저장한다. 다만 **항상** 운영 DB와 다른 파일(`<DB이름>.learning.<확장자>`)에 쓴다.

#### 기대 결과
운영 DB에서 사용자 질문에 쓰는 데이터는 현재 주뿐이다. 학습용 1년치 데이터는 운영 DB에 섞이지 않는다.

#### 예외 처리
게시물 하나가 실패해도 나머지 게시물은 계속 처리한다. 실패 사유는 통계의 오류 목록에 남긴다.

#### 관련 구현
`src/menu_bot/pipeline.py`, `src/menu_bot/db.py`, `src/menu_bot/cli.py`

#### 관련 테스트
TEST-STORE-001

### REQ-QUERY-001 질문에서 날짜·끼니 읽기

#### 목적
평소 말투의 질문에서 날짜와 끼니를 읽는다.

#### 입력
사용자 발화, 기준 시각(`TIMEZONE` 기준).

#### 선행 조건
없음.

#### 동작
- 끼니: `아침/조식` → 조식, `점심/중식` → 중식, `저녁/석식` → 석식.
- 끼니를 말하지 않아도 코너 이름으로 끼니를 알 수 있으면 그 끼니로 본다. `간편식`은 조식, `건강식`은 중식이다.
  이유: 1년치 데이터를 모두 살펴보니 이 두 코너가 다른 끼니에 나온 적이 0회였다.
- 날짜: `오늘/내일/모레/어제`, `M월 D일`, `M/D`, `요일`. 요일은 월~일 어느 날에 물어도
  **현재 주**의 그 요일로 본다.
- 끼니 없이 요일만 말하면 그날 조식·중식·석식 전부로 본다.
- `금욜`, `낼`, `담주`, `차주`, `금주` 같은 줄임말은 표준 표현으로 바꾼 뒤 해석한다.

#### 기대 결과
발화 하나가 `(날짜, 끼니 또는 전체)` 한 쌍으로 정해진다.

#### 예외 처리
해석하지 못한 요청은 아무거나 고르지 않고 REQ-QUERY-002로 넘긴다.

#### 관련 구현
`src/menu_bot/query.py`

#### 관련 테스트
TEST-QUERY-001

### REQ-QUERY-002 답할 수 없는 질문 되묻기

#### 목적
답할 수 없는 질문에는 엉뚱한 답을 주지 않고, 무엇이 문제인지 되묻는다.

#### 입력
사용자 발화, 기준 날짜, 알고 있는 사업장 이름 목록.

#### 선행 조건
없음.

#### 동작
다음 질문에는 메뉴를 보여주지 않고 안내로 답한다.

- 지난 주 · 다음 주 · 다다음 주 요청 → 이번 주만 조회할 수 있다고 알린다.
- 모르는 낱말이 섞인 질문 → 못 알아들은 낱말을 따옴표로 되짚어 주고 사용방법을 보여준다.
  날짜만 알아들었다고 그날 전체 식단으로 넘기지 않는다.
- 세 끼니에 모두 나오는 코너(`일반식`, `PLUS`)만 말한 질문 → 끼니를 되묻는다.
- 코너와 끼니가 서로 맞지 않는 질문 → 그 코너가 나오는 끼니를 알려준다.
- 날짜 · 요일 · 끼니 · 코너가 둘 이상 섞였거나, 주차 표현과 날짜 표현이 함께 온 질문 →
  하나만 말해 달라고 되묻는다.
- 존재하지 않는 날짜 → 날짜를 확인해 달라고 되묻는다.
- 요일이 빠진 `이번주 …` 질문 → 요일을 함께 말해 달라고 안내한다.

낱말은 두 갈래로 나눠 판단한다.

- 날짜·요일·끼니·코너·사업장은 질문의 뼈대다. 읽어 낸 뒤 지우고, 되살리지 않는다.
- 말투·조사는 모르는 낱말에 붙어 있을 수 있다. 그래서 되살려서 모르는 낱말을 온전히 보여준다.

> **예시** `삼각김밥`을 `삼각김`으로 되묻지 않는다.

#### 기대 결과
답할 수 없는 질문에는 사유와 사용법이 돌아간다. 지원하는 표현에는 되묻지 않는다.

#### 예외 처리
사업장 이름 목록은 설정(`POST_PREFIXES`)에서 받아 "아는 말"로 다룬다.

#### 관련 구현
`src/menu_bot/query.py`

#### 관련 테스트
TEST-QUERY-002

### REQ-QUERY-003 식단 응답 본문 만들기

#### 목적
확정된 질문에 대해 응답 본문을 만든다. 현재 주 밖의 식단은 보여주지 않는다.

#### 입력
확정된 날짜·끼니, 운영 DB, 기준 시각.

#### 선행 조건
REQ-QUERY-002를 통과한 질문.

#### 동작
- 대상 날짜가 **이번 주가 아니면** 조회하지 않고, 이번 주만 조회할 수 있다고 답한다.
- 대상 날짜가 토·일이면 실제 날짜와 함께 식당을 운영하지 않는다고 알린다.
- 데이터가 없으면 지어내지 않는다. "아직 그룹웨어에 올라오지 않았다, 올라오면 자동 반영된다"고 답한다.
- 응답 본문은 맨 위에 `MM월 DD일(요일)` 제목을 두고, 그 아래 끼니별 구획을 둔다.
  각 끼니 안에서는 코너별로 항목을 세로로 나열한다.
- PLUS 코너는 `공통 PLUS`로 묶어 맨 뒤에 둔다.
- 미운영 안내가 있는 끼니는 메뉴 대신 안내 문구만 보여준다. 특식 코너에는 표시를 붙인다.
- 사업장은 응답에 드러내지 않는다. 공통 식단을 고른다.
  공통 식단을 가려낼 수 없으면 담당자 확인이 필요하다고 답한다.

#### 기대 결과
모든 답변에 실제 날짜가 들어간다. 현재 주 밖의 데이터는 어떤 경로로도 보이지 않는다.

#### 예외 처리
`주말`이라고만 물으면 날짜를 해석하지 않고 미운영을 알린다.

#### 관련 구현
`src/menu_bot/query.py`

#### 관련 테스트
TEST-QUERY-003

### REQ-API-001 카카오 챗봇 Skill 응답

#### 목적
카카오 챗봇 Skill 서버로서 요청을 받고, 카카오 규격에 맞는 응답을 돌려준다.

#### 입력
카카오 SkillPayload(POST), URL 경로의 웹훅 토큰, `KAKAO_WEBHOOK_TOKEN`.

#### 선행 조건
서버가 로컬 주소에 열려 있고, 터널을 거쳐 바깥에 공개된다.

#### 동작
- 토큰이 설정돼 있으면 경로의 토큰이 맞을 때만 처리한다. 맞지 않으면 404로 답한다.
- 발화가 비었거나 도움말 요청(`도움`, `사용방법`, 인사말 등)이면 사용방법을 돌려준다.
- 식단 질문으로 보이지 않는 발화는 DB를 열지 않고 사용방법을 돌려준다.
- 하루 전체 응답은 끼니별 말풍선으로 나눈다. 카카오가 정한 출력 개수 상한(3개)을 넘기지 않는다.
- 말풍선 하나가 길이 상한을 넘으면 잘라내고, 잘렸다는 것을 알린다.
- 모든 응답에 `사용방법` 퀵 리플라이(말풍선 아래 버튼)를 붙인다.
- 상태 확인용 주소는 저장된 항목 수도 함께 돌려준다.

#### 기대 결과
SkillResponse 2.0 형식의 JSON. 카카오 요청을 처리하는 동안에는 그룹웨어에 접속하지 않는다.

#### 예외 처리
DB 연결은 요청마다 열고, 반드시 닫는다.

#### 관련 구현
`src/menu_bot/web.py`

#### 관련 테스트
TEST-API-001

### REQ-WATCH-001 다음 주 식단표를 주말에 미리 받기

#### 목적
다음 주 식단표가 주말에 올라오면 바로 받아 둔다. 그래야 월요일 0시부터 바로 답할 수 있다.

#### 입력
기준 시각, `NEXT_WEEK_STATE_PATH`의 상태 파일.

#### 선행 조건
없음.

#### 동작
1. 대상 주차는 **다음 주 월요일**이다.
2. 이미 그 주차를 확보했으면 그룹웨어에 접속하지 않고 바로 끝낸다.
3. 아직이면 게시물 제목만 가볍게 읽어, 대상 주차 게시물이 있는지 본다.
4. 없으면 시도 기록만 남기고 끝낸다.
5. 있으면 그 자리에서 이미지 내려받기·OCR·파싱·저장까지 마치고, `week_start`를 다음 주 월요일로 둔다.

주말에 미리 저장돼 있어도 월요일 전에는 사용자에게 보이지 않는다.
이유: 응답은 실제 달력 날짜로 거른다.

**저장된 메뉴 항목이 1건 이상일 때만 확보로 본다.**
이유: 제목만 올라오고 이미지가 없거나 OCR이 아무것도 읽지 못한 경우까지 성공으로 치면, 주말 확인이 멈춰 그 주 식단을 통째로 놓친다.

확보되면 대상 주차마다 성공 알림을 한 번만 보낸다. 상태는 대상 주차 단위로 저장한다.
그래서 주차가 바뀌면 손으로 초기화하지 않아도 저절로 다시 확인한다.

#### 기대 결과
상태 파일에 대상 주차 · 확보 여부 · 시도 기록(결과 구분과 사유)이 남는다.
같은 주말의 남은 확인은 그룹웨어에 다시 접속하지 않는다.

#### 예외 처리
- 접속·수집 실패와 항목 0건은 확보로 보지 않는다. 사유와 함께 시도 기록에 남긴다.
- 상태 파일이 깨져 있어도 수집을 막지 않는다. 새 상태로 시작한다.
- 시도 기록에는 상한을 두어 끝없이 커지지 않게 한다.
- 성공 알림을 보내지 못해도 수집 자체는 성공으로 처리한다.
- 정기 수집이 같은 게시물을 다시 처리해도 괜찮다.
  이유: REQ-STORE-001이 게시물 단위로 바꿔 넣는다.

#### 관련 구현
`src/menu_bot/next_week_watch.py`, `src/menu_bot/scraper.py`

#### 관련 테스트
TEST-WATCH-001

### REQ-WATCH-002 식단을 못 받았을 때 마감 점검

#### 목적
주말 확인이 끝난 뒤에도 식단을 받지 못한 상태를 그대로 두지 않는다.

#### 입력
기준 시각, 상태 파일, 운영 DB.

#### 선행 조건
없음.

#### 동작
마감 판정:

1. 실행된 시점에 대상 주차가 확보됐는지만 본다. 언제가 마감인지는 코드가 정하지 않고 타이머가 정한다.
2. 확보되지 않았으면 무엇이 몇 번 실패했는지 정리해 알림 메일을 보낸다.
3. 같은 주차에는 두 번 보내지 않는다.
4. **발송에 실패하면 보냈다고 기록하지 않는다.** 그래서 다음 실행에서 다시 보낸다.

시간별 재시도:

1. 지금 채워져 있어야 할 주차를 정한다. 토·일에는 다가오는 월요일의 주, 월~금에는 그 주 월요일의 주다.
2. 그 주차 식단이 **DB에** 없으면 다시 수집한다.
3. 이미 있으면 그룹웨어에 접속하지 않고 바로 끝낸다.

확보 여부는 상태 파일로 보지 않고 DB로 본다.
이유: 정기 수집은 상태 파일을 건드리지 않고 DB만 채우는 경우가 있다.

알림 메일:

- SMTP 설정(`SMTP_HOST`, `SMTP_PORT`, `SMTP_SECURITY`, `SMTP_USER`,
  `SMTP_PASSWORD`, `NOTIFY_EMAIL_FROM`, `NOTIFY_EMAIL_TO`)이 갖춰졌을 때만 보낸다.
- 설정이 비어 있으면 보내지 않고 기록만 남긴다.
- 비밀번호는 어디에도 출력하지 않는다.

#### 기대 결과
확보돼 있으면 아무 일도 하지 않고 끝난다. 확보되지 않은 상태는 사람이 볼 수 있는 알림으로 드러난다.

#### 예외 처리
종료 코드는 사람이 봐야 하는 일만 실패로 표시한다. 그래야 서비스 관리자가 로그로 잡을 수 있다.

- 게시글이 아직 없는 것은 정상이다. 성공으로 끝낸다.
- 접속 실패, 항목 0건, 마감까지 확보하지 못함은 실패로 끝낸다.

기준 시각은 시스템 시계를 바꾸지 않고 인자로 넣어 검증할 수 있어야 한다.

#### 관련 구현
`src/menu_bot/next_week_watch.py`, `src/menu_bot/notify.py`, `src/menu_bot/cli.py`

#### 관련 테스트
TEST-WATCH-002

## 6. 비기능 요구사항

### NFR-SEC-001 비밀값·실제 데이터를 밖에 내보내지 않기

**비밀값과 실제 데이터는 저장소·로그·응답에 나가지 않는다.**

- 계정·비밀번호·웹훅 토큰·SMTP 비밀번호는 `.env`에만 두고, Git이 추적하지 않게 한다.
- 진단 출력은 값을 보여주지 않는다. **비어 있는 환경변수의 키 이름만** 알린다.
- 내부 주소, 원본 이미지, OCR 결과, SQLite DB, 어휘집, 게시물 목록은 공개 저장소에 넣지 않는다.
- 웹훅 URL에는 긴 임의 토큰을 붙인다. 토큰이 설정돼 있으면 맞지 않는 요청은 404로 답한다.
- OCR은 모든 과정이 이 컴퓨터 안에서 돈다. 이미지나 인식 결과를 밖으로 보내지 않는다.
- 서버에서 API는 루프백 주소에만 연다. 바깥 공개는 터널로만 한다.

측정: Git이 추적하는 파일에 실제 계정·토큰·내부 주소가 있는지 값을 맞대어 검사한다. 발견 0건이어야 한다.

### NFR-OPS-001 사람 없이 정해진 시각에 돌고 스스로 복구

**정해진 시각에 사람 손 없이 돌고, 멈추면 스스로 다시 켜진다.**

| 작업 | 실행 시점 |
|---|---|
| 정기 수집 | 주 1회, 월요일 아침 |
| 다음 주 확인 | 금요일 밤 1회, 주말에는 2시간 간격 |
| 마감 점검 | 일요일 밤 1회 |
| 미확보 재시도 | 평일 1시간 간격 |

- 웹 서버와 터널은 항상 켜 두는 유닛으로 따로 두고, 각각 따로 자동 재시작한다.
  이유: 둘을 묶으면 한쪽이 재시작할 때 다른 쪽이 다시 살아날 근거가 사라진다.
- 확보된 뒤의 재시도는 바깥에 접속하지 않고 바로 끝나야 한다.
  이유: 타이머는 스스로를 끌 수 없다. 그래서 "반복 끝내기"를 아무 일도 하지 않는 것으로 만든다.
- 수집·OCR 작업에는 메모리 상한을 둔다.
  이유: 같은 장비의 다른 서비스를 밀어내지 않게 한다.

측정: 상시 유닛을 강제로 끄면 스스로 다시 켜진다. 확보된 주차의 재시도는 그룹웨어에 접속하지 않고 끝난다.

### NFR-PORT-001 macOS·Windows·Linux 동일 결과

**macOS · Windows · Linux에서 같은 결과를 낸다.**

- 플랫폼에 따라 달라지는 곳은 OCR provider와 브라우저 채널 선택뿐이다. 파서부터 뒤쪽은 플랫폼을 모른다.
- provider를 바꾸는 변경에는 그 플랫폼의 시험용 입력(fixture)과 실제 이미지 비교 근거가 있어야 한다.
- 날짜·시각은 시스템 시간대와 관계없이 항상 `TIMEZONE` 설정값으로 해석한다.
  운영 서버의 시스템 시간대가 달라도 조회 결과가 바뀌면 안 된다.
- 콘솔이 이모지를 처리하지 못하는 환경에서도 CLI 출력 때문에 프로그램이 멈추면 안 된다.

측정: 두 provider로 같은 이미지를 처리했을 때 칸 구성과 휴무·특식 판정이 같다.

## 7. 데이터 사양

| 데이터 | 형태·위치 | 보존 |
|---|---|---|
| 게시물 | SQLite `source_posts`: 게시물 번호(기본 키, PK), 제목, 사업장, 주 시작일, 이미지 URL 목록, 처리 시각 | 운영 DB에 계속 쌓인다 |
| 메뉴 항목 | SQLite `menu_entries`: 날짜, 사업장, 끼니, 코너, 메뉴 글자, 상태, 출처(게시물·제목·이미지 URL), 신뢰도. `(날짜, 사업장, 끼니, 코너, 게시물)` 조합은 겹치지 않고, 빨리 찾기 위한 색인을 둔다 | 운영 수집은 현재 주만 저장 |
| 원본 이미지 | 로컬 파일. 파일 이름은 이미지 URL의 해시 값 | 다시 수집·다시 해석할 때 쓰려고 로컬에 둔다 |
| OCR 캐시 | 이미지 옆의 OCR 결과 JSON 파일 | 교정 규칙을 고친 뒤 다시 만들 수 있다 |
| 어휘집 | JSON(메뉴 이름 → 등장 횟수). 경로는 `MENU_VOCAB_PATH` | 실제 메뉴 데이터이므로 저장소에 넣지 않는다 |
| 주말 감시 상태 | JSON. 경로는 `NEXT_WEEK_STATE_PATH`. 대상 주차, 확보 여부·시각, 시도 기록, 알림 보낸 주차를 담는다 | 대상 주차가 바뀌면 시도 기록을 새로 시작 |
| 학습용 DB | 운영 DB와 다른 파일 | 검증에만 쓰고, 운영 응답에 쓰지 않는다 |

개인정보는 다루지 않는다. 계정 정보는 데이터로 보지 않고 설정으로 보며, `.env`에만 둔다.

## 8. Configuration 사양

모든 설정은 환경변수이며 `.env`에서 읽는다. 값 예시는 `.env.example`에 있다.

| 키 | 필수 | 기본값 | 용도 |
|---|---|---|---|
| `GROUPWARE_USER` / `GROUPWARE_PASSWORD` | 수집 시 필수 | 없음 | 그룹웨어 로그인 |
| `GROUPWARE_URL` | 수집 시 필수 | 없음 | 게시판 진입 주소 |
| `POST_PREFIXES` | 선택 | 사업장 4곳 | 대상 게시물 제목의 앞머리, 질문에서 "아는 말" 판정 |
| `DEFAULT_LOCATION` | 선택 | 빈 값 | 기본 사업장(13절 참조) |
| `DATABASE_PATH` | 선택 | data 디렉터리의 SQLite 파일 | 운영 SQLite 경로 |
| `TIMEZONE` | 선택 | `Asia/Seoul` | 모든 날짜·시각 해석 기준 |
| `KAKAO_WEBHOOK_TOKEN` | 운영 시 필수 | 빈 값 | 웹훅 경로 토큰. 비면 토큰 검사를 하지 않음 |
| `OCR_PROVIDER` | 선택 | `auto` | `auto` / `apple_vision` / `paddleocr` |
| `PADDLE_DET_MODEL` / `PADDLE_REC_MODEL` / `PADDLE_ENABLE_MKLDNN` | 선택 | 검증된 조합 | PaddleOCR 모델·가속 선택 |
| `BROWSER_CHANNEL` | 선택 | `auto` | Playwright 브라우저 채널 |
| `NEXT_WEEK_STATE_PATH` | 선택 | data 디렉터리의 JSON | 주말 감시 상태 파일 |
| `MENU_VOCAB_PATH` | 선택 | data 디렉터리의 JSON | 어휘집 경로 |
| `SMTP_HOST` / `SMTP_PORT` / `SMTP_SECURITY` / `SMTP_USER` / `SMTP_PASSWORD` / `SMTP_TIMEOUT` | 알림 시 필수 | 보안 방식에 따른 포트 기본값 | 알림 메일 발송 |
| `NOTIFY_EMAIL_FROM` / `NOTIFY_EMAIL_TO` | 알림 시 필수 | 발신자는 계정으로 대신함 | 알림 수신자 |
| `MENUBOT_PORT` | 선택 | 운영 스크립트 기본값 | 서버 포트 |
| `NGROK_DOMAIN` | 선택 | 빈 값 | 고정 터널 도메인 |

값만 비워 둔 환경변수는 "지정하지 않음"으로 보고 기본값을 쓴다.
설정이 부족하면 **키 이름만** 알리고, 값은 출력하지 않는다.

## 9. 오류 처리 정책

| 상황 | 처리 |
|---|---|
| 계정 정보 미설정 | 그룹웨어에 접속하기 전에 바로 멈춤 |
| 로그인 중 화면 넘김(리다이렉트) 때문에 나는 오류 | 결국 마지막 화면에 도착하므로 무시하고 화면이 자리 잡기를 기다림 |
| 그룹웨어 접속·수집 실패 | 사유를 상태 파일 시도 기록에 남기고 실패로 끝냄(재시도는 다음 예정 시각) |
| 게시물 1건 처리 실패 | 나머지 게시물은 계속 처리하고 통계의 오류 목록에 남김(일부만 성공해도 됨) |
| 식단표가 아닌 이미지 | 오류로 보지 않고 "제외한 이미지"로 셈 |
| 게시글은 있는데 항목 0건 | **확보로 처리하지 않고** 사유를 남긴 뒤 다음 예정 시각에 다시 시도 |
| 상태 파일 손상 | 오류로 보지 않고 새 상태로 시작 |
| 알림 메일 미설정 | 발송을 건너뛰고 기록만 남김(실패로 보지 않음) |
| 성공 알림 발송 실패 | 수집은 성공으로 처리(데이터를 버리지 않음) |
| 마감 알림 발송 실패 | 발송 기록을 남기지 않아 다음 실행에서 다시 시도 |
| 조회할 데이터 없음 | 지어내지 않고 아직 게시되지 않았다고 안내 |
| 공통 식단을 가려낼 수 없음 | 아무거나 고르지 않고 담당자 확인이 필요하다고 안내 |
| 해석할 수 없거나 애매한 질문 | 아무거나 고르지 않고 되묻기(REQ-QUERY-002) |
| 콘솔 인코딩 미지원 | CLI 출력 인코딩을 UTF-8로 맞추고, 맞출 수 없으면 알리지 않고 건너뜀 |

다시 시도하는 일은 사람이 하지 않고 타이머가 한다. "언제 다시 시도하는가"를 코드에 넣지 않는다.
종료 코드는 **사람이 봐야 하는 일**만 실패로 표시한다.

## 10. 보안 요구사항

- 인증 수단(그룹웨어 계정, 웹훅 토큰, SMTP 앱 비밀번호)은 `.env`에만 둔다. 그 파일은 소유자만 쓸 수 있게 권한을 제한한다.
- 저장소에는 `.env.example`의 키 이름과 형식만 둔다.
- 저장소에 넣으면 안 되는 것: `.env`, 내부 주소, 계정, 원본 이미지, OCR 결과, SQLite DB,
  어휘집, 게시물 목록, 터널 인증 설정, 로그.
- 카카오 요청을 처리하는 동안에는 그룹웨어에 로그인하지 않는다. 수집 경로와 응답 경로를 나눈다.
- 웹훅 경로 토큰이 설정돼 있으면, 맞지 않는 요청은 본문을 읽지 않고 404로 답한다.
- API는 루프백에만 연다. 바깥 공개는 터널로만 한다.
- 비밀번호·토큰을 로그·응답·문서·커밋 메시지에 출력하지 않는다.
- 발견한 비밀값(Secret)은 마음대로 지우지 않는다. 드러내지 않고 위치만 알린다.

## 11. 테스트 사양

ID 규칙: `TEST-<CATEGORY>-NNN`.

검증 명령은 `.venv/bin/python -m pytest -q`와 `.venv/bin/python -m compileall -q src tests` 두 가지다.

### TEST-PARSE-001

#### 검증 대상
REQ-PARSE-001, REQ-CORRECT-001

#### 선행 조건
어휘집 교정을 끈 상태(빈 어휘집).

#### 절차
`tests/test_corrections.py`의 파서 통합 테스트를 쓴다. 만들어 낸 OCR 라인 목록을 파서에 넣는다.
목록에는 날짜 머리글 · 끼니 이름표 · 코너 이름표 · 메뉴 라인 · 잡음 조각이 들어 있다.

#### Expected Result
`(끼니, 코너)`별 항목이 만들어진다. 그 안의 오인식은 바로잡히고, 잡음 조각은 사라진다.

### TEST-CORRECT-001

#### 검증 대상
REQ-CORRECT-001

#### 선행 조건
없음. 어휘집이 필요한 경우는 임시 파일로 만든다.

#### 절차
`tests/test_corrections.py`. 아래 경우를 표로 나열해 결과를 고정한다.

- 잡음 판정
- 고정 표기 교정(늘 같은 식으로 틀리는 계통 오류 포함)
- 규칙과 글자가 겹치는 실제 메뉴가 깨지지 않는지
- 빈도가 비슷한 표기 차이를 건드리지 않는지
- 어휘집 교정의 길이·후보 수·최소 등장 횟수 조건
- 어휘집 파일이 없을 때·깨졌을 때·예전 형식일 때
- 교정 후 중복 제거와 순서 유지

#### Expected Result
의도한 항목만 바뀌고 나머지는 그대로다. 어휘집이 없거나 깨져도 오류가 나지 않는다.

### TEST-STORE-001

#### 검증 대상
REQ-STORE-001

#### 선행 조건
없음.

#### 절차
1. `tests/test_query.py`의 주차 거르기 테스트로 지난 주·다음 주 항목이 걸러지는지 확인한다.
2. `tests/test_cli.py`로, 어떤 운영 DB 경로를 주더라도 학습용 DB 경로가 그것과 같은 파일이 되지 않는지 확인한다.

#### Expected Result
대상 주차 월~일 항목만 남고, 제외 개수가 맞는다. 학습용 경로는 운영 경로와 항상 다르다.

### TEST-QUERY-001

#### 검증 대상
REQ-QUERY-001

#### 선행 조건
기준 시각을 고정한다.

#### 절차
`tests/test_query.py`, `tests/test_query_routing.py`. 아래 입력을 표로 나열해 해석 결과를 확인한다.

- 요일+끼니
- 끼니만
- 요일만
- 줄임말
- 코너 이름만(간편식·건강식)

#### Expected Result
각 발화가 기대한 날짜와 끼니로 해석된다. 코너 이름만 말한 질문은 그 코너가 나오는 끼니만 보여준다.

### TEST-QUERY-002

#### 검증 대상
REQ-QUERY-002

#### 선행 조건
메뉴 데이터가 들어 있는 임시 DB.

#### 절차
`tests/test_query.py`, `tests/test_query_routing.py`. 아래 입력을 표로 나열한다.

- 지난 주·다음 주 요청
- 모르는 낱말이 섞인 질문
- 코너 중복, 코너와 끼니가 맞지 않는 질문
- 날짜·요일·끼니 중복
- 잘못된 날짜
- 요일이 빠진 `이번주 …`
- 식단 질문이 아닌 잡담

#### Expected Result
각 입력에 기대한 안내 문구가 돌아가고 메뉴는 보이지 않는다.
모르는 낱말은 조사가 붙은 채로 잘리지 않고 온전히 인용된다. 지원하는 표현에는 되묻지 않는다.

### TEST-QUERY-003

#### 검증 대상
REQ-QUERY-003

#### 선행 조건
메뉴 데이터가 들어 있는 임시 DB와 고정된 기준 시각.

#### 절차
`tests/test_query.py`, `tests/test_query_routing.py`. 아래를 확인한다.

- 현재 주 밖 날짜 요청
- 주말 요청
- 데이터가 없는 날짜 요청
- 코너별 세로 목록 응답 형식

#### Expected Result
- 현재 주 밖 데이터는 어떤 경로로도 보이지 않는다.
- 주말과 아직 게시되지 않은 상태는 실제 날짜가 들어간 안내로 답한다.
- 응답 본문이 끼니 → 코너 → 항목 구조를 유지한다.

### TEST-API-001

#### 검증 대상
REQ-API-001

#### 선행 조건
없음.

#### 절차
`tests/test_query.py`, `tests/test_query_routing.py`.

1. 하루 전체 응답을 카카오 응답으로 바꿔 말풍선이 나뉘는지 확인한다.
2. 사용방법 문구가, 앞에 안내 문구가 붙은 경우까지 말풍선 하나에 들어가는지 확인한다.

#### Expected Result
끼니별로 말풍선이 나뉘고 개수 상한을 넘지 않는다. 사용방법은 한 말풍선에 들어간다.

### TEST-WATCH-001

#### 검증 대상
REQ-WATCH-001

#### 선행 조건
임시 DB·상태 파일과 가짜 수집기·메일 발송기.

#### 절차
`tests/test_next_week_watch.py`. 아래 경우를 하나씩 확인한다.

- 게시글을 찾은 뒤 확보·알림
- 같은 주차 알림 중복 방지
- 게시글은 있으나 항목 0건
- 게시글을 찾지 못함
- 수집기 예외
- 이미 확보된 주차의 접속 생략
- 주차가 바뀐 뒤 다시 확인
- 상태 파일 손상

#### Expected Result
확보 판정은 저장된 항목 수에 따라 갈린다. 시도 기록에 결과 구분과 사유가 남는다.
이미 확보된 주차에서는 그룹웨어에 접속하지 않는다.

### TEST-WATCH-002

#### 검증 대상
REQ-WATCH-002

#### 선행 조건
임시 DB·상태 파일과 가짜 메일 발송기.

#### 절차
`tests/test_next_week_watch.py`. 아래를 확인한다.

- 마감 시점에 확보하지 못했을 때의 알림
- 알림에 실패 사유 요약이 들어가는지
- 확보된 주차에는 알림이 없는지
- 같은 주차에 1회만 보내는지
- 발송에 실패하면 다시 시도하는지
- 자정을 넘겨도 대상 주차가 흔들리지 않는지
- DB 기준 확보 판정
- 항목 0건에서 확보로 넘어가지 않는지
- 메일을 설정하지 않았을 때 발송을 건너뛰는지

#### Expected Result
확보된 상태에서는 아무 알림이 없다. 확보하지 못한 상태는 사유가 담긴 알림으로 한 번만 드러난다.
발송에 실패하면 다음 실행에서 다시 시도한다.

### TEST-PARSE-002

#### 검증 대상
REQ-PARSE-002

#### 선행 조건
없음. 그룹웨어도 OCR provider도 거치지 않는다. OCR 줄을 직접 만들어 파서만 검사한다.

> **참고** 14절에서 자동 테스트를 붙이기로 한 REQ-PARSE-002가 이 검사다.
> 저장된 OCR 캐시는 쓰지 않는다. 캐시를 저장소에 둘지는 아직 정하지 않았다(14절).

#### 절차
`tests/test_parse_status.py`를 실행한다 (`.venv/bin/python -m pytest`).

#### Expected Result
- 대체휴무·공휴일·전사휴무는 그날 세 끼 모두에 미운영으로 들어간다.
- `조식 미제공`은 조식에만 들어간다.
- 영양사 픽 같은 문구는 미운영으로 보지 않고 특식으로 본다.
- 평범한 식단은 `normal`이다.
  이유: 이 마지막 확인이 없으면, 분류기가 무엇이든 미운영이라고 답해도 앞의 검사들이 통과한다.

### TEST-SCRAPE-001

#### 검증 대상
REQ-SCRAPE-001

#### 선행 조건
사내망 접속과 그룹웨어 계정이 필요하다. 자동화할 수 없다.
이유: 바깥 시스템의 로그인과 게시판 구조에 기대는데, 그 구조는 이쪽에서 고정할 수 없다.

#### 절차
운영 수집 스크립트(`scripts/run-linux-collect.sh`, `scripts/run-windows-collect.ps1`)와 같은 두 명령을 차례로 쓴다.

1. `.venv/bin/menu-bot scrape --pages 2 --output data/latest_manifest.json`을 실행한다.
   목록 2쪽에서 식단 게시물을 찾아 게시물 번호·제목·이미지 URL을 이 파일에 저장한다.
2. 출력의 게시물 수를 보고, 저장된 파일에서 게시물 제목과 이미지 URL을 확인한다.
3. `.venv/bin/menu-bot ingest data/latest_manifest.json`을 실행한다.
   이미지를 내려받아 OCR하고 저장한 뒤, 게시물·이미지·항목 수와 오류 목록을 출력한다.
4. 3번을 한 번 더 실행해 게시물 단위로 바꿔 넣는지 본다.

#### Expected Result
현재 주 게시물을 찾아 이미지를 모두 내려받는다. 다시 실행해도 항목 수가 같고 중복 항목이 생기지 않는다.

### TEST-OCR-001

#### 검증 대상
REQ-OCR-001, NFR-PORT-001(provider 비교 부분)

#### 선행 조건
플랫폼별 OCR provider가 설치돼 있어야 한다(macOS Apple Vision, Windows·Linux PaddleOCR).
두 플랫폼이 모두 있어야 하므로 자동화하지 않는다.

#### 절차
같은 이미지를 두 provider로 처리하고 `<이미지>.ocr.json` 캐시를 비교한다.

#### Expected Result
좌표 규칙(비율로 바꾼 x·y·width·height)이 같고, 칸 구성과 휴무·특식 판정이 같다.

### TEST-OPS-001

#### 검증 대상
NFR-SEC-001, NFR-OPS-001, NFR-PORT-001

#### 선행 조건
없음. 로그인 정보도 네트워크도 쓰지 않는다.

이 검사는 세 요구사항의 '측정' 가운데 기계가 답할 수 있는 부분만 맡는다. 나머지는 이렇게 나눈다.

- 상시 유닛을 강제로 끈 뒤 스스로 다시 켜지는지: 아래 수동 절차
- 두 provider의 실제 이미지 비교: TEST-OCR-001

#### 절차
1. `tests/test_operational_contracts.py`를 실행한다 (`.venv/bin/python -m pytest`).
2. 운영 서버에서 `systemctl kill menubot-web`을 한 뒤, 유닛이 스스로 다시 올라오는지 본다(수동).

#### Expected Result
- `.env`·DB·원본 이미지·어휘집이 Git에 추적되지 않고, git이 무시 대상으로 판정한다.
- 웹훅 토큰이 맞지 않으면 404다. 설정이 없는 환경에서는 소스 코드를 살피는 확인이 대신 고정한다. 건너뛰었다면 그 사실이 출력에 남는다.
- 상시 유닛 둘은 서로 다른 파일이고, 각각 `Restart=always`다.
- 수집·다음 주 확인 유닛에는 메모리 상한이 있다.
- 타이머마다 짝이 되는 서비스가 있다.
- 시간대는 설정값에서 온다. 파서·조회 코드에 시간대 없는 `datetime.now()`가 없다.

## 12. 요구사항 추적성

| Requirement | Implementation | Test | Status |
|---|---|---|---|
| REQ-SCRAPE-001 | `src/menu_bot/scraper.py` | TEST-SCRAPE-001 (수동) | implemented |
| REQ-OCR-001 | `src/menu_bot/ocr_provider.py` | TEST-OCR-001 (수동) | implemented |
| REQ-PARSE-001 | `src/menu_bot/parser.py` | TEST-PARSE-001 | verified |
| REQ-PARSE-002 | `src/menu_bot/parser.py` | TEST-PARSE-002 | verified |
| REQ-CORRECT-001 | `src/menu_bot/corrections.py` | TEST-CORRECT-001, TEST-PARSE-001 | verified |
| REQ-STORE-001 | `src/menu_bot/pipeline.py` | TEST-STORE-001 | verified |
| REQ-QUERY-001 | `src/menu_bot/query.py` | TEST-QUERY-001 | verified |
| REQ-QUERY-002 | `src/menu_bot/query.py` | TEST-QUERY-002 | verified |
| REQ-QUERY-003 | `src/menu_bot/query.py` | TEST-QUERY-003 | verified |
| REQ-API-001 | `src/menu_bot/web.py` | TEST-API-001 | verified |
| REQ-WATCH-001 | `src/menu_bot/next_week_watch.py` | TEST-WATCH-001 | verified |
| REQ-WATCH-002 | `src/menu_bot/next_week_watch.py` | TEST-WATCH-002 | verified |
| NFR-SEC-001 | `src/menu_bot/web.py`, `.gitignore` | TEST-OPS-001 | verified |
| NFR-OPS-001 | `scripts/systemd/menubot-web.service`, `scripts/systemd/menubot-tunnel.service`, `scripts/systemd/menubot-collect.service` | TEST-OPS-001 | implemented |
| NFR-PORT-001 | `src/menu_bot/ocr_provider.py`, `src/menu_bot/config.py` | TEST-OPS-001, TEST-OCR-001 (수동) | implemented |

Status 값: `draft` (사양만 있음) / `implemented` / `verified` (실제 실행까지 확인) /
`deprecated`.

## 13. 미확정 사항

- **`DEFAULT_LOCATION`의 사양이 없다.** 설정 항목이 있고 CLI·API가 이 값을 인자로 넘기는 경로도 있지만,
  응답을 만들 때는 쓰이지 않는다. 사업장별 응답을 하지 않기로 한 범위(2절)와 이어지는 문제다.
  이 설정을 남길지 없앨지 확인이 필요하다.
- **공통 식단을 고르는 기준이 코드에 고정돼 있다.** 특정 사업장 이름 하나를 공통 식단으로 보고,
  그 이름이 없으면 사업장이 한 곳일 때만 그 결과를 쓴다. 이 이름이 `POST_PREFIXES`의 첫 항목을
  뜻하는지, 고정된 값인지 문서에 없다.
- **운영 DB에서 지난 주 데이터를 언제 지울지 정해져 있지 않다.** 저장은 게시물 단위로 바꿔 넣고
  조회는 현재 주로 제한하므로, 사용자에게는 보이지 않지만 지난 주 게시물의 항목이 DB에 계속 남는다.
  정리 주기·방법을 정할지 확인이 필요하다.
- **CLI `ask`와 카카오 API의 "아는 사업장 이름" 목록이 다르다.** API는 설정값을 넘기고
  CLI는 기본 목록을 쓴다. 두 경로가 같은 판정을 해야 하는지 확인이 필요하다.
- **서버를 재부팅했을 때 저절로 켜지는지는 아직 검증하지 않았다**(`README.md`에 적혀 있다). 유닛은
  부팅할 때 올라오도록 등록돼 있지만, 재부팅해서 확인하는 일이 남아 있다.

## 14. 향후 개선 후보

- 터널 무료 한도에 가까워지면, 바깥 공개 경로를 서버리스 무료 주소로 옮긴다.
- Windows에서 운영할 때 필요한 터널 프로그램의 최소 버전 조건은 사내 IT 승인 절차로 해결한다.
- `REQ-PARSE-002`는 2026-09-22에 자동 테스트를 붙였다(TEST-PARSE-002). 남은 둘
  (`REQ-SCRAPE-001` · `REQ-OCR-001`)은 수동 절차로 둔다.
  이유: 각각 사내망 그룹웨어와 플랫폼별 OCR provider가 있어야 확인할 수 있다.
- 위 둘을 자동화하려면, 먼저 저장된 OCR 캐시를 저장소에 둘 수 있는 형태(개인정보·내부 주소 제거)로
  만들지 결정해야 한다.
