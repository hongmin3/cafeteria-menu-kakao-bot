# 뷰밥 메뉴 알리미 사양서

<!-- spec-template: v1 -->

| 항목 | 값 |
|---|---|
| Document Version | 1.0.0 |
| Project Version | 0.1.0 |
| Last Updated | 2026-09-19 |
| Status | active |
| Owner | hongmin |

이 문서는 `cafeteria-menu-kakao-bot`가 **어떻게 동작해야 하는가**를 정의하는 기준이다.
코드가 현재 그렇게 동작한다는 사실은 사양이 아니다. 사양과 구현이 다르면 코드에 맞춰 이
문서를 고치지 말고 `SPEC / CODE MISMATCH`로 식별한다 — 절차는 `AGENTS.md`에 있다.

프로젝트 규모에 따라 아래 Section은 줄일 수 있다. 다만 **1 목적 · 2 범위 · 5 기능 요구사항 ·
9 오류 처리 · 11 테스트 사양 · 12 추적성**은 유지보수형 자동화라면 반드시 채운다.

## 1. 목적

사내 구성원이 카카오톡에서 "금요일 점심"처럼 평소 말투로 물으면, 그룹웨어 자유게시판에
이미지로만 올라오는 주간 식단표를 대신 읽어 그날 그 끼니의 메뉴를 알려준다. 사람이 게시판을
찾아 들어가 이미지를 확대해 읽는 일을 없애는 것이 목적이다.

성공 조건: 근무일(월~금) 업무 시작 시각에 그 주 식단이 DB에 들어와 있고, 카카오 질문에
**실제 날짜가 명시된** 메뉴 응답 또는 사유가 분명한 안내가 돌아간다.

## 2. 프로젝트 범위

### 포함

- 그룹웨어 자유게시판에서 주간 식단 게시물과 본문 이미지 수집 (Playwright)
- 플랫폼별 OCR provider(macOS Apple Vision / Windows·Linux PaddleOCR)로 이미지 인식
- 표 좌표 기반으로 날짜 · 끼니(조식·중식·석식) · 코너(일반식·PLUS 코너·간편식·건강식) 구조화
- OCR 글자 오인식과 노이즈 조각 교정
- SQLite에 **현재 주** 식단 저장
- FastAPI 카카오 Skill API로 자연어 질문 해석과 응답
- 주말 폴링으로 다음 주 식단표를 미리 확보하고, 미확보 시 알림 메일 발송·시간별 재시도

### 제외

- **과거 주·다음 주 식단 조회.** 사용자에게는 현재 주만 제공한다. 1년치 과거 데이터는
  OCR·파서 검증(`--learning` 모드) 전용이며 운영 DB와 파일을 공유하지 않는다.
- **주말 식단.** 식당을 운영하지 않으므로 안내 문구만 돌려준다.
- **사업장별 식단 분기 응답.** 공통 식단만 노출하고 지점명을 응답에 드러내지 않는다.
- **없는 정보의 추측·생성.** 식단표가 아직 없으면 지어내지 않고 사정을 그대로 알린다.
- 카카오 유료 Event API(선제 푸시), 사용자 인증·개인화, 식단 예약·주문 기능.

## 3. 시스템 구성

| 구성 요소 | 파일 | 책임 |
|---|---|---|
| 수집기 | `src/menu_bot/scraper.py` | 그룹웨어 로그인, 게시물 제목·본문 이미지 URL 수집 |
| OCR | `src/menu_bot/ocr.py`, `src/menu_bot/ocr_provider.py`, `src/menu_bot/vision_ocr.swift` | 플랫폼별 provider 선택, 공통 좌표계로 정규화, 결과 캐시 |
| 파서 | `src/menu_bot/parser.py` | OCR 라인 → 날짜·끼니·코너 셀 배정, 휴무·특식 판정 |
| 교정 | `src/menu_bot/corrections.py` | 노이즈 제거, 고정 표기 교정, 어휘집 교정 |
| 파이프라인 | `src/menu_bot/pipeline.py` | 이미지 다운로드, 중복 제거, 주차 필터, 저장 |
| 저장소 | `src/menu_bot/db.py`, `src/menu_bot/models.py` | SQLite 스키마와 조회 |
| 질의 | `src/menu_bot/query.py` | 질문 정규화·해석·되묻기, 응답 문구 생성 |
| API | `src/menu_bot/web.py` | 카카오 Skill 엔드포인트, 토큰 검사, 말풍선 구성 |
| CLI | `src/menu_bot/cli.py` | `scrape` · `ingest` · `ask` · `check-next-week` · `next-week-deadline` · `ensure-menu` · `notify-test` · `build-vocab` |
| 주말 감시 | `src/menu_bot/next_week_watch.py`, `src/menu_bot/notify.py` | 다음 주 확보 판정, 상태 파일, 알림 메일 |
| 설정 | `src/menu_bot/config.py`, `.env.example` | 환경변수 로딩 |
| 운영 | `scripts/systemd/`, `scripts/register-linux-services.sh` | 서비스·타이머 등록 |

외부 시스템 경계는 세 곳이다 — 그룹웨어(읽기 전용, 로그인 필요), 카카오 챗봇 Skill 서버(수신),
SMTP 서버(알림 발송). 이 밖으로 이미지나 인식 결과를 내보내지 않는다.

## 4. 전체 동작 흐름

수집(월요일 08:00 정기 + 주말 폴링 + 시간별 재시도):

1. 그룹웨어에 로그인하고 자유게시판 목록을 연다.
2. `POST_PREFIXES`로 시작하는 제목의 게시물만 고른다.
3. 게시물 본문에서 식단표로 볼 수 있는 큰 이미지의 URL을 모은다.
4. 이미지를 내려받아 OCR한다(결과는 이미지 옆의 OCR 결과 JSON 파일에 캐시).
5. OCR 라인의 좌표로 날짜 열 · 끼니 행 · 코너 행을 잡고 셀마다 메뉴 항목을 배정한다.
6. 항목을 교정·중복 제거하고, 운영 수집이면 대상 주차(월~일) 밖의 항목을 버린다.
7. 게시물 단위로 SQLite에 덮어쓴다(같은 `source_post_id`는 항상 교체).

응답:

1. 카카오가 발화를 담은 Skill 요청을 보낸다.
2. 토큰을 검사하고, 도움말 요청·식단 질문이 아닌 발화를 먼저 걸러낸다.
3. 발화를 정규화해 날짜·요일·끼니·코너를 읽고, 모호하거나 지원하지 않는 요청이면 되묻는다.
4. 현재 주 평일이면 DB를 조회해 끼니 → 코너 → 항목 순의 세로 목록을 만든다.
5. 끼니별 말풍선으로 나눠 SkillResponse 2.0 형식으로 돌려준다.

## 5. 기능 요구사항

ID 규칙: `REQ-<CATEGORY>-NNN`. CATEGORY는 대문자·숫자, NNN은 세 자리.
한 번 부여한 ID는 재사용하거나 의미를 바꾸지 않는다. 삭제한 ID를 다른 기능에 돌려쓰지 않는다.

기능 그룹 — CATEGORY마다 사람이 읽는 이름이다. `docs/SPEC.html`의 기능 목록이 이 이름으로 묶인다.

| 카테고리 | 이름 |
|---|---|
| SCRAPE | 게시판 수집 |
| OCR | 이미지 인식 |
| PARSE | 식단표 해석 |
| CORRECT | 메뉴 보정 |
| STORE | 저장 |
| QUERY | 질문 해석·응답 |
| API | 챗봇 서버 |
| WATCH | 다음 주 식단 감시 |
| SEC | 보안 |
| OPS | 운영 |
| PORT | 이식성 |

### REQ-SCRAPE-001 게시판 식단표 이미지 수집

#### 목적
그룹웨어 자유게시판에서 식단 게시물과 본문 식단표 이미지의 URL을 얻는다.

#### 입력
`GROUPWARE_URL`, `GROUPWARE_USER`, `GROUPWARE_PASSWORD`, `POST_PREFIXES`, `BROWSER_CHANNEL`,
조회할 목록 페이지 수.

#### 선행 조건
계정 정보가 설정돼 있고 그룹웨어에 네트워크로 닿는다.

#### 동작
headless 브라우저로 로그인한 뒤 자유게시판 목록을 연다. 제목이 `[<사업장>]` 형태로
`POST_PREFIXES` 중 하나로 시작하는 게시물만 대상으로 삼는다. 대상 게시물을 열어 본문의
큰 이미지(가로·세로 500px 초과, 게시판 첨부 이미지 URL 패턴)만 식단표 후보로 모은다.
제목만 필요한 경우(주말 폴링)에는 게시물 본문에 들어가지 않는 가벼운 목록 조회를 쓴다.

#### 기대 결과
게시물 번호 · 제목 · 이미지 URL 목록이 담긴 항목의 배열.

#### 예외 처리
- 계정 정보가 비어 있으면 즉시 중단한다(그룹웨어에 접속하지 않는다).
- 로그인 화면이 여러 번 리다이렉트하며 발생하는 navigation 중단 오류는 최종 화면에
  도달하므로 무시하고 정착 시간을 더 준다. 그 밖의 브라우저 오류는 그대로 올린다.
- 브라우저는 플랫폼에 맞게 고른다. Chrome이 없는 Linux 서버에서는 번들 Chromium을 쓰고,
  비특권 user namespace가 막힌 환경을 위해 샌드박스를 끈다.

#### 관련 구현
`src/menu_bot/scraper.py`, `src/menu_bot/config.py`

#### 관련 테스트
(없음)

### REQ-OCR-001 OCR 결과 공통 좌표 규약

#### 목적
플랫폼과 무관하게 파서가 같은 좌표 규약으로 OCR 결과를 받는다.

#### 입력
이미지 파일 경로, `OCR_PROVIDER`(`auto` | `apple_vision` | `paddleocr`).

#### 선행 조건
선택된 provider의 실행 환경이 갖춰져 있다(macOS는 Swift 툴체인, Windows·Linux는 PaddleOCR).

#### 동작
`auto`는 macOS에서 Apple Vision, 그 밖에서 PaddleOCR을 고른다. 두 provider 모두
`{"text", "confidence", "x", "y", "width", "height"}` 라인 목록을 돌려주며, 좌표는 0~1로
정규화하고 원점은 **이미지 좌측 하단**, y축은 위로 증가한다. 좌측 상단 픽셀 좌표를 쓰는
provider는 이 규약으로 변환해 내보낸다. 인식 결과는 이미지 옆의 OCR 결과 JSON 파일에 캐시하고,
캐시가 있으면 다시 인식하지 않는다.

#### 기대 결과
동일 이미지에 대해 provider가 달라도 파서가 같은 셀 구성·휴무/특식 판정을 얻는다.

#### 예외 처리
- 알 수 없는 `OCR_PROVIDER` 값은 오류로 중단한다.
- PaddleOCR 모델명과 가속 옵션은 장비마다 되는 조합이 달라 코드가 아니라 환경변수로
  바꿀 수 있어야 한다. 값만 비운 환경변수는 "미지정"으로 보고 기본값을 쓴다.
- 감지·인식 모델명을 둘 다 명시한다. 하나만 지정하면 한국어 인식 모델이 조용히 바뀐다.

#### 관련 구현
`src/menu_bot/ocr.py`, `src/menu_bot/ocr_provider.py`, `src/menu_bot/vision_ocr.swift`

#### 관련 테스트
(없음)

### REQ-PARSE-001 식단표 날짜·끼니·코너 구조화

#### 목적
식단표 이미지의 OCR 라인을 날짜 · 끼니 · 코너로 구조화한다.

#### 입력
게시물 정보(사업장, 주 시작일), 이미지 URL, OCR 라인 목록.

#### 선행 조건
OCR 라인이 REQ-OCR-001의 좌표 규약을 따른다.

#### 동작
표 상단의 날짜 헤더로 날짜 열을, 표 좌측의 끼니·코너 라벨로 행을 잡는다. 각 메뉴 라인은
가장 가까운 날짜 열과 자신이 속한 끼니·코너 행에 배정한다. 끼니 라벨은 세로 병합 셀
중앙에 있어 단순 최근접 배정이 틀리므로, 각 끼니는 `일반식`으로 시작한다는 표 구조를
기준으로 코너 행을 끼니에 묶고 그 경계로 배정한다. 게시물 제목에서 사업장과 주 시작일을
읽고, 두 자리 날짜 표기는 주 시작일에 가장 가까운 해로 해석한다.

#### 기대 결과
`(날짜, 사업장, 끼니, 코너)`마다 하나의 메뉴 항목 문자열.

#### 예외 처리
- 제목에서 사업장 또는 날짜를 읽을 수 없으면 그 게시물은 오류로 기록하고 건너뛴다.
- 날짜 헤더나 끼니 라벨을 찾지 못한 이미지(안내 포스터 등)는 식단표가 아니므로 0건을
  돌려주고, 파이프라인이 "제외한 이미지"로 센다.
- 주 시작일에서 10일 넘게 떨어진 날짜, 표 바깥 좌표의 라인, 로고·장식 문구는 버린다.
- 같은 셀이 여러 이미지에서 겹치면 더 긴 결과를 채택한다. 없는 코너를 만들지 않는다.

#### 관련 구현
`src/menu_bot/parser.py`, `src/menu_bot/pipeline.py`, `src/menu_bot/models.py`

#### 관련 테스트
TEST-PARSE-001

### REQ-PARSE-002 미운영·특식 상태 구분

#### 목적
미운영·특식을 메뉴 항목이 아니라 상태로 구분한다.

#### 입력
REQ-PARSE-001이 배정한 셀 텍스트.

#### 선행 조건
없음.

#### 동작
- 대체휴무 · 공휴일 · 전사휴무처럼 **하루 전체**를 뜻하는 문구는 그날 조식·중식·석식
  세 끼 모두에 `안내` 코너의 미운영 항목으로 넣는다.
- `조식 미제공`, `석식 운영 없음`처럼 끼니가 명시된 문구는 **그 끼니에만** 넣는다.
- 특식 · DAY · 영양사 픽 같은 문구가 든 셀은 특식 상태로 표시한다.
- 그 밖은 일반 상태다.

#### 기대 결과
각 항목이 `normal` · `no_service` · `special` 중 하나의 상태를 갖고, 응답에서 미운영은
안내 문구로, 특식은 별도 표시로 구분된다.

#### 예외 처리
휴무 안내 그림 안의 큰 숫자를 날짜 헤더로 오인하지 않는다. 특식 때문에 비어 있는 코너를
추측해 채우지 않는다.

#### 관련 구현
`src/menu_bot/parser.py`

#### 관련 테스트
(없음)

### REQ-CORRECT-001 메뉴 이름 오인식 보정

#### 목적
사용자에게 그대로 보여줄 메뉴 이름에서 OCR 오인식과 노이즈 조각을 걷어낸다.

#### 입력
셀에서 모은 항목 문자열 목록, 선택적 어휘집(`MENU_VOCAB_PATH`).

#### 선행 조건
없음. 어휘집이 없어도 동작해야 한다.

#### 동작
세 겹으로 처리한다.

1. **노이즈 제거** — 한글이 한 글자도 없는 항목은 버린다.
2. **고정 표기 교정** — 한국어에 없는 표기를 고정 대응표로 바꾼다. 대응표는 실제 이미지로
   두 provider 결과를 대조해 확인한 것만 넣고, 빈도가 비슷해 원문일 수 있는 표기 차이는
   손대지 않는다.
3. **어휘집 교정** — 어휘집에 없는 항목이 길이가 같고 한 글자만 다른 어휘집 항목과
   대응하며 후보가 **딱 하나**일 때만 바꾼다. 어휘집 자체에도 오인식이 섞이므로 일정 횟수
   이상 등장한 항목만 교정 후보로 쓴다.

교정 후 중복은 순서를 유지한 채 없앤다. 어휘집을 새로 만들 때는 어휘집 교정을 끈다.

#### 기대 결과
교정 규칙이 의도한 항목만 바꾸고, 규칙과 글자가 겹치는 실제 메뉴 이름을 깨뜨리지 않는다.

#### 예외 처리
어휘집 파일이 없거나 깨졌거나 예전 형식(목록)이어도 오류가 아니다. 어휘집은 실제 메뉴
데이터이므로 저장소에 넣지 않는다. 원본 OCR 결과는 캐시에 남아 있어 규칙을 고친 뒤 다시
만들 수 있다.

#### 관련 구현
`src/menu_bot/corrections.py`, `src/menu_bot/cli.py`

#### 관련 테스트
TEST-CORRECT-001, TEST-PARSE-001

### REQ-STORE-001 현재 주 식단 저장

#### 목적
운영 DB에는 현재 주 식단만 두고, 검증용 과거 데이터와 파일 단위로 분리한다.

#### 입력
파싱 결과, 대상 주차의 월요일, `DATABASE_PATH`, `--learning` 여부.

#### 선행 조건
없음.

#### 동작
운영 수집은 대상 주차 월~일 범위 밖의 항목을 저장 전에 버리고, 버린 개수를 통계로 낸다.
저장은 게시물 단위로 교체한다 — 같은 `source_post_id`의 기존 항목을 지우고 새로 넣으므로
같은 게시물을 여러 번 처리해도 안전하다. `--learning`은 모든 날짜를 저장하되 **항상**
운영 DB와 다른 파일(`<DB이름>.learning.<확장자>`)에 쓴다.

#### 기대 결과
운영 DB에서 사용자 질문에 쓰이는 데이터는 현재 주뿐이고, 학습용 1년치 데이터가 운영 DB에
섞이지 않는다.

#### 예외 처리
게시물 하나가 실패해도 나머지 게시물 처리는 계속하고, 실패 사유를 통계의 오류 목록에 남긴다.

#### 관련 구현
`src/menu_bot/pipeline.py`, `src/menu_bot/db.py`, `src/menu_bot/cli.py`

#### 관련 테스트
TEST-STORE-001

### REQ-QUERY-001 질문에서 날짜·끼니 해석

#### 목적
평소 말투의 질문에서 날짜와 끼니를 읽는다.

#### 입력
사용자 발화, 기준 시각(`TIMEZONE` 기준).

#### 선행 조건
없음.

#### 동작
- 끼니: `아침/조식` → 조식, `점심/중식` → 중식, `저녁/석식` → 석식.
- 끼니를 말하지 않아도 코너 이름으로 알 수 있으면 그 끼니로 본다 — `간편식` → 조식,
  `건강식` → 중식. 이 매핑은 1년치 전수 조사에서 다른 끼니 등장이 0회인 것에 근거한다.
- 날짜: `오늘/내일/모레/어제`, `M월 D일`, `M/D`, `요일`. 요일은 월~일 어느 날에 물어도
  **현재 주**로 해석한다.
- 끼니 없이 요일만 말하면 그날 조식·중식·석식 전부를 뜻한다.
- `금욜`, `낼`, `담주`, `차주`, `금주` 같은 줄임말을 표준 표현으로 바꾼 뒤 해석한다.

#### 기대 결과
발화 하나가 `(날짜, 끼니 또는 전체)` 한 쌍으로 확정된다.

#### 예외 처리
해석하지 못한 요청은 임의로 고르지 않는다 — REQ-QUERY-002로 넘긴다.

#### 관련 구현
`src/menu_bot/query.py`

#### 관련 테스트
TEST-QUERY-001

### REQ-QUERY-002 답할 수 없는 질문 되묻기

#### 목적
답할 수 없는 질문에 엉뚱한 답을 주는 대신 무엇이 문제인지 되묻는다.

#### 입력
사용자 발화, 기준 날짜, 알고 있는 사업장 이름 목록.

#### 선행 조건
없음.

#### 동작
다음은 메뉴를 보여주지 않고 안내로 답한다.

- 지난 주 · 다음 주 · 다다음 주 요청 → 이번 주만 조회할 수 있다고 알린다.
- 모르는 낱말이 섞인 질문 → 못 알아들은 낱말을 따옴표로 되짚어 주고 사용방법을 보여준다.
  날짜만 알아들었다고 그날 전체 식단으로 넘기지 않는다.
- 세 끼니에 모두 나오는 코너(`일반식`, `PLUS`)만 말한 질문 → 끼니를 되묻는다.
- 코너와 끼니가 서로 맞지 않는 질문 → 그 코너가 나오는 끼니를 알려준다.
- 날짜 · 요일 · 끼니 · 코너가 둘 이상 섞였거나 주차 표현과 날짜 표현이 함께 온 질문 →
  하나만 말해 달라고 되묻는다.
- 존재하지 않는 날짜 → 날짜를 확인해 달라고 되묻는다.
- 요일이 빠진 `이번주 …` 질문 → 요일을 함께 말해 달라고 안내한다.

낱말 판정은 두 층으로 나눈다. 날짜·요일·끼니·코너·사업장은 질문의 뼈대이므로 지운 뒤
되돌리지 않고, 말투·조사는 모르는 낱말에 붙어 있을 수 있으므로 되살려 낱말을 온전히
보여준다(`삼각김밥`을 `삼각김`으로 되묻지 않는다).

#### 기대 결과
답할 수 없는 질문에는 사유와 사용법이 돌아가고, 지원하는 표현에는 되묻기가 발생하지 않는다.

#### 예외 처리
사업장 이름 목록은 설정(`POST_PREFIXES`)에서 받아 "아는 말"로 취급한다.

#### 관련 구현
`src/menu_bot/query.py`

#### 관련 테스트
TEST-QUERY-002

### REQ-QUERY-003 식단 응답 본문 생성

#### 목적
확정된 질문에 대해 현재 주 경계를 지키며 응답 본문을 만든다.

#### 입력
확정된 날짜·끼니, 운영 DB, 기준 시각.

#### 선행 조건
REQ-QUERY-002를 통과한 질문.

#### 동작
- 대상 날짜가 **이번 주가 아니면** 조회하지 않고 이번 주만 조회할 수 있다고 답한다.
- 대상 날짜가 토·일이면 실제 날짜와 함께 식당 미운영을 알린다.
- 데이터가 없으면 지어내지 않고 "아직 그룹웨어에 올라오지 않았다, 올라오면 자동 반영된다"고
  답한다.
- 응답 본문은 `MM월 DD일(요일)` 제목 뒤에 끼니별 구획을 두고, 각 끼니 안에서 코너별
  세로 목록으로 항목을 나열한다. PLUS 코너는 `공통 PLUS`로 묶어 맨 뒤에 둔다.
- 미운영 안내가 있는 끼니는 메뉴 대신 안내 문구만 보여준다. 특식 코너에는 표시를 붙인다.
- 사업장은 응답에 노출하지 않는다. 공통 식단을 고르고, 공통 식단을 특정할 수 없으면
  담당자 확인이 필요하다고 답한다.

#### 기대 결과
모든 답변에 실제 날짜가 들어가고, 현재 주 밖의 데이터는 어떤 경로로도 노출되지 않는다.

#### 예외 처리
`주말`이라고만 물으면 날짜 해석 없이 미운영을 알린다.

#### 관련 구현
`src/menu_bot/query.py`

#### 관련 테스트
TEST-QUERY-003

### REQ-API-001 카카오 챗봇 Skill 응답

#### 목적
카카오 챗봇 Skill 서버로서 요청을 받아 규격에 맞는 응답을 돌려준다.

#### 입력
카카오 SkillPayload(POST), URL 경로의 웹훅 토큰, `KAKAO_WEBHOOK_TOKEN`.

#### 선행 조건
서버가 로컬 주소에 바인딩돼 있고 터널을 통해 공개된다.

#### 동작
- 토큰이 설정돼 있으면 경로의 토큰이 일치할 때만 처리하고, 아니면 404로 답한다.
- 발화가 비었거나 도움말 요청(`도움`, `사용방법`, 인사말 등)이면 사용방법을 돌려준다.
- 식단 질문으로 보이지 않는 발화는 DB를 열지 않고 사용방법을 돌려준다.
- 하루 전체 응답은 끼니별 말풍선으로 나눈다. 카카오의 출력 개수 상한(3개)을 넘기지 않고,
  말풍선 하나가 길이 상한을 넘으면 잘라내고 생략됐음을 알린다.
- 모든 응답에 `사용방법` 퀵 리플라이를 붙인다.
- 상태 확인용 엔드포인트는 저장된 항목 수를 함께 돌려준다.

#### 기대 결과
SkillResponse 2.0 형식의 JSON. 카카오 요청 처리 중에는 그룹웨어에 접속하지 않는다.

#### 예외 처리
DB 연결은 요청마다 열고 반드시 닫는다.

#### 관련 구현
`src/menu_bot/web.py`

#### 관련 테스트
TEST-API-001

### REQ-WATCH-001 다음 주 식단표 주말 확보

#### 목적
다음 주 식단표가 주말에 게시되는 즉시 확보해, 월요일 0시부터 바로 응답할 수 있게 한다.

#### 입력
기준 시각, `NEXT_WEEK_STATE_PATH`의 상태 파일.

#### 선행 조건
없음.

#### 동작
대상 주차는 **다음 주 월요일**이다. 이미 그 주차를 확보한 상태면 그룹웨어에 접속하지 않고
즉시 끝낸다. 아직이면 게시물 **제목만** 가볍게 읽어 대상 주차 게시물이 있는지 본다. 없으면
시도 기록만 남기고 끝낸다. 있으면 그 자리에서 이미지 다운로드·OCR·파싱·저장까지 마치고
`week_start`를 다음 주 월요일로 둔다. 응답은 실제 달력 날짜로 걸러지므로 주말에 미리
저장돼 있어도 월요일 전에는 노출되지 않는다.

**저장된 메뉴 항목이 1건 이상일 때만 확보로 본다.** 제목만 올라오고 이미지가 없거나 OCR이
아무것도 읽지 못한 경우를 성공으로 처리하면 폴링이 멈춰 그 주 식단을 통째로 놓친다.

확보되면 대상 주차당 한 번만 성공 알림을 보낸다. 상태는 대상 주차 단위로 저장하므로 주차가
바뀌면 수동 초기화 없이 자동으로 다시 확인한다.

#### 기대 결과
상태 파일에 대상 주차 · 확보 여부 · 시도 기록(결과 구분과 사유)이 남고, 같은 주말의 남은
폴링은 그룹웨어에 다시 접속하지 않는다.

#### 예외 처리
- 접속·스크래핑 실패, 항목 0건은 확보로 보지 않고 사유와 함께 시도 기록에 남긴다.
- 상태 파일이 깨져 있어도 수집을 막지 않는다 — 새로 시작한다.
- 시도 기록은 상한을 두어 무한히 커지지 않게 한다.
- 성공 알림 발송이 실패해도 수집 자체는 성공으로 처리한다.
- 정기 수집이 같은 게시물을 다시 처리해도 REQ-STORE-001의 교체 저장으로 안전하다.

#### 관련 구현
`src/menu_bot/next_week_watch.py`, `src/menu_bot/scraper.py`

#### 관련 테스트
TEST-WATCH-001

### REQ-WATCH-002 식단 미확보 마감 점검

#### 목적
주말 폴링이 끝난 뒤에도 식단을 못 받은 상태를 방치하지 않는다.

#### 입력
기준 시각, 상태 파일, 운영 DB.

#### 선행 조건
없음.

#### 동작
- **마감 판정**: 호출된 시점에 대상 주차가 확보됐는지만 본다. "언제가 마감인가"는 코드가
  아니라 타이머가 정한다. 미확보면 무엇이 몇 번 실패했는지 정리해 알림 메일을 보내고,
  같은 주차에 두 번 보내지 않는다. **발송이 실패하면 기록하지 않아** 다음 실행에서 다시
  시도한다.
- **시간별 재시도**: 지금 채워져 있어야 할 주차(토·일은 다가오는 월요일, 월~금은 그 주
  월요일)의 식단이 **DB에** 없으면 다시 수집한다. 확보 여부를 상태 파일이 아니라 DB로
  판단해야 한다 — 정기 수집이 상태 파일을 건드리지 않고 DB만 채우는 경로가 있기 때문이다.
  이미 있으면 그룹웨어에 접속하지 않고 즉시 끝난다.
- 알림 메일은 SMTP 설정(`SMTP_HOST`, `SMTP_PORT`, `SMTP_SECURITY`, `SMTP_USER`,
  `SMTP_PASSWORD`, `NOTIFY_EMAIL_FROM`, `NOTIFY_EMAIL_TO`)이 갖춰졌을 때만 보내고, 비어
  있으면 건너뛰고 기록만 남긴다. 비밀번호는 어디에도 출력하지 않는다.

#### 기대 결과
확보되면 아무 일도 하지 않고 조용히 끝나며, 미확보 상태는 사람이 볼 수 있는 알림으로 드러난다.

#### 예외 처리
종료 코드는 사람이 봐야 하는 사건만 실패로 표시한다 — 게시글이 아직 없는 것은 정상이므로
성공으로 끝내고, 접속 실패와 항목 0건, 미확보 마감은 실패로 끝내 서비스 관리자가 로그로
잡을 수 있게 한다. 기준 시각은 시스템 시계를 바꾸지 않고 인자로 주입해 검증할 수 있어야 한다.

#### 관련 구현
`src/menu_bot/next_week_watch.py`, `src/menu_bot/notify.py`, `src/menu_bot/cli.py`

#### 관련 테스트
TEST-WATCH-002

## 6. 비기능 요구사항

### NFR-SEC-001 비밀값·실데이터 비노출

**비밀값과 실데이터가 저장소·로그·응답에 나가지 않는다.**

- 계정·비밀번호·웹훅 토큰·SMTP 비밀번호는 `.env`에만 두고 Git 추적 대상에서 제외한다.
  진단 출력은 값이 아니라 **비어 있는 환경변수 키 이름만** 보고한다.
- 내부 주소, 원본 이미지, OCR 결과, SQLite DB, 어휘집, 게시물 목록은 공개 저장소에 넣지 않는다.
- 웹훅 URL에는 긴 임의 토큰을 붙이고, 토큰이 설정되면 일치하지 않는 요청은 404로 답한다.
- OCR은 전 과정이 로컬에서 실행되며 이미지나 인식 결과를 외부로 전송하지 않는다.
- 서버에서 API는 루프백 주소에만 바인딩하고 외부 노출은 터널을 통해서만 한다.

측정: Git 추적 파일에 실제 계정·토큰·내부 주소가 없는지 값 대조로 검사해 발견 0건.

### NFR-OPS-001 무인 정기 실행과 자동 복구

**정해진 시각에 사람 개입 없이 돌고, 죽으면 스스로 살아난다.**

- 정기 수집은 주 1회(월요일 아침), 다음 주 확인은 금요일 밤 1회와 주말 2시간 간격,
  마감 점검은 일요일 밤 1회, 미확보 재시도는 평일 1시간 간격이다.
- 웹 서버와 터널은 상시 유닛으로 두고 각각 독립적으로 자동 재시작한다. 둘을 묶으면 한쪽이
  재시작할 때 다른 쪽이 되살아날 근거를 잃는다.
- 확보된 뒤의 재시도는 외부 접속 없이 즉시 끝나야 한다(타이머는 스스로를 끌 수 없으므로,
  "루프 종료"를 아무 일도 하지 않는 것으로 구현한다).
- 수집·OCR 작업에는 메모리 상한을 걸어 같은 장비의 다른 서비스를 밀어내지 않는다.

측정: 상시 유닛을 강제 종료했을 때 자동 복구되고, 확보된 주차의 재시도는 그룹웨어 접속
없이 종료된다.

### NFR-PORT-001 macOS·Windows·Linux 동일 결과

**macOS · Windows · Linux에서 같은 결과를 낸다.**

- 플랫폼 차이는 OCR provider와 브라우저 채널 선택에 국한하고, 파서 이하는 플랫폼을 모른다.
  provider를 바꾸는 변경에는 해당 플랫폼 fixture와 실제 이미지 비교 근거가 필요하다.
- 날짜·시각 해석은 시스템 시간대와 무관하게 항상 `TIMEZONE` 설정값을 명시해 쓴다. 운영
  서버의 시스템 시간대가 다르더라도 조회 결과가 달라지지 않아야 한다.
- 콘솔 인코딩이 이모지를 처리하지 못하는 환경에서도 CLI 출력이 죽지 않아야 한다.

측정: 두 provider로 같은 이미지를 처리했을 때 셀 구성과 휴무·특식 판정이 일치한다.

## 7. 데이터 사양

| 데이터 | 형태·위치 | 보존 |
|---|---|---|
| 게시물 | SQLite `source_posts` — 게시물 번호(PK), 제목, 사업장, 주 시작일, 이미지 URL 목록, 처리 시각 | 운영 DB에 누적 |
| 메뉴 항목 | SQLite `menu_entries` — 날짜, 사업장, 끼니, 코너, 메뉴 텍스트, 상태, 출처(게시물·제목·이미지 URL), 신뢰도. `(날짜, 사업장, 끼니, 코너, 게시물)`이 고유하고 조회용 색인을 둔다 | 운영 수집은 현재 주만 저장 |
| 원본 이미지 | 로컬 파일. 파일명은 이미지 URL 해시 | 재수집·재파싱용 로컬 보관 |
| OCR 캐시 | 이미지 옆의 OCR 결과 JSON 파일 | 교정 규칙을 고친 뒤 재생성 가능 |
| 어휘집 | JSON(메뉴 이름 → 등장 횟수). 경로는 `MENU_VOCAB_PATH` | 실제 메뉴 데이터이므로 저장소 제외 |
| 주말 감시 상태 | JSON. 경로는 `NEXT_WEEK_STATE_PATH` — 대상 주차, 확보 여부·시각, 시도 기록, 알림 발송 주차 | 대상 주차가 바뀌면 시도 기록을 새로 시작 |
| 학습용 DB | 운영 DB와 다른 파일 | 검증 전용, 운영 응답에 쓰지 않음 |

개인정보는 다루지 않는다. 계정 정보는 데이터가 아니라 설정이며 `.env`에만 둔다.

## 8. Configuration 사양

모든 설정은 환경변수이며 `.env`에서 읽는다. 값 예시는 `.env.example`에 있다.

| 키 | 필수 | 기본값 | 용도 |
|---|---|---|---|
| `GROUPWARE_USER` / `GROUPWARE_PASSWORD` | 수집 시 필수 | 없음 | 그룹웨어 로그인 |
| `GROUPWARE_URL` | 수집 시 필수 | 없음 | 게시판 진입 주소 |
| `POST_PREFIXES` | 선택 | 사업장 4곳 | 대상 게시물 제목 접두사, 질의에서 "아는 말" 판정 |
| `DEFAULT_LOCATION` | 선택 | 빈 값 | 기본 사업장(13절 참조) |
| `DATABASE_PATH` | 선택 | data 디렉터리의 SQLite 파일 | 운영 SQLite 경로 |
| `TIMEZONE` | 선택 | `Asia/Seoul` | 모든 날짜·시각 해석 기준 |
| `KAKAO_WEBHOOK_TOKEN` | 운영 시 필수 | 빈 값 | 웹훅 경로 토큰. 비면 토큰 검사를 하지 않음 |
| `OCR_PROVIDER` | 선택 | `auto` | `auto` / `apple_vision` / `paddleocr` |
| `PADDLE_DET_MODEL` / `PADDLE_REC_MODEL` / `PADDLE_ENABLE_MKLDNN` | 선택 | 검증된 조합 | PaddleOCR 모델·가속 선택 |
| `BROWSER_CHANNEL` | 선택 | `auto` | Playwright 브라우저 채널 |
| `NEXT_WEEK_STATE_PATH` | 선택 | data 디렉터리의 JSON | 주말 감시 상태 파일 |
| `MENU_VOCAB_PATH` | 선택 | data 디렉터리의 JSON | 어휘집 경로 |
| `SMTP_HOST` / `SMTP_PORT` / `SMTP_SECURITY` / `SMTP_USER` / `SMTP_PASSWORD` / `SMTP_TIMEOUT` | 알림 시 필수 | 보안 방식에 따른 포트 기본값 | 알림 메일 발송 |
| `NOTIFY_EMAIL_FROM` / `NOTIFY_EMAIL_TO` | 알림 시 필수 | 발신자는 계정으로 대체 | 알림 수신자 |
| `MENUBOT_PORT` | 선택 | 운영 스크립트 기본값 | 서버 포트 |
| `NGROK_DOMAIN` | 선택 | 빈 값 | 고정 터널 도메인 |

값만 비워 둔 환경변수는 "미지정"으로 보고 기본값을 쓴다. 설정이 부족할 때는 **키 이름만**
알리고 값을 출력하지 않는다.

## 9. 오류 처리 정책

| 상황 | 처리 |
|---|---|
| 계정 정보 미설정 | 그룹웨어에 접속하기 전에 즉시 중단 |
| 로그인 리다이렉트 경합 오류 | 최종 화면에 도달하므로 무시하고 정착 대기 |
| 그룹웨어 접속·스크래핑 실패 | 사유를 상태 파일 시도 기록에 남기고 실패 종료(재시도는 다음 예정 시각) |
| 게시물 1건 처리 실패 | 나머지 게시물 처리를 계속하고 통계의 오류 목록에 남김(부분 성공 허용) |
| 식단표가 아닌 이미지 | 오류가 아니라 "제외한 이미지"로 집계 |
| 게시글은 있는데 항목 0건 | **확보로 처리하지 않고** 사유를 남긴 뒤 다음 예정 시각에 재시도 |
| 상태 파일 손상 | 오류로 보지 않고 새 상태로 시작 |
| 알림 메일 미설정 | 발송을 건너뛰고 기록만 남김(실패로 보지 않음) |
| 성공 알림 발송 실패 | 수집은 성공으로 처리(데이터를 버리지 않음) |
| 마감 알림 발송 실패 | 발송 기록을 남기지 않아 다음 실행에서 재시도 |
| 조회 대상 데이터 없음 | 지어내지 않고 아직 게시되지 않았음을 안내 |
| 공통 식단 특정 불가 | 임의로 고르지 않고 담당자 확인이 필요하다고 안내 |
| 해석 불가·모호한 질문 | 임의로 고르지 않고 되묻기(REQ-QUERY-002) |
| 콘솔 인코딩 미지원 | CLI 출력 인코딩을 UTF-8로 조정하고, 조정할 수 없으면 조용히 건너뜀 |

재시도는 사람이 아니라 타이머가 한다. "언제 다시 시도하는가"를 코드에 넣지 않는다.
종료 코드는 **사람이 봐야 하는 사건**만 실패로 표시한다.

## 10. 보안 요구사항

- 인증 수단(그룹웨어 계정, 웹훅 토큰, SMTP 앱 비밀번호)은 `.env`에만 두고 권한을 소유자
  전용으로 제한한다. 저장소에는 `.env.example`의 키 이름과 형식만 둔다.
- 저장소에 들어가면 안 되는 것: `.env`, 내부 주소, 계정, 원본 이미지, OCR 결과, SQLite DB,
  어휘집, 게시물 목록, 터널 인증 설정, 로그.
- 카카오 요청을 처리하는 동안에는 그룹웨어에 로그인하지 않는다(수집 경로와 응답 경로 분리).
- 웹훅 경로 토큰이 설정되면 불일치 요청은 본문을 읽지 않고 404로 답한다.
- API는 루프백에만 바인딩하고, 외부 노출은 터널로만 한다.
- 비밀번호·토큰을 로그·응답·문서·커밋 메시지에 출력하지 않는다. 발견한 Secret은 임의로
  지우지 않고 노출 없이 위치만 알린다.

## 11. 테스트 사양

ID 규칙: `TEST-<CATEGORY>-NNN`.

검증 명령은 `.venv/bin/python -m pytest -q`와 `.venv/bin/python -m compileall -q src tests`다.

### TEST-PARSE-001

#### 검증 대상
REQ-PARSE-001, REQ-CORRECT-001

#### 선행 조건
어휘집 교정을 끈 상태(빈 어휘집).

#### 절차
`tests/test_corrections.py`의 파서 통합 테스트. 날짜 헤더 · 끼니 라벨 · 코너 라벨 ·
메뉴 라인 · 노이즈 조각으로 이루어진 합성 OCR 라인 목록을 파서에 넣는다.

#### Expected Result
`(끼니, 코너)`별 항목이 만들어지고, 그 안의 오인식이 교정되며 노이즈 조각은 사라진다.

### TEST-CORRECT-001

#### 검증 대상
REQ-CORRECT-001

#### 선행 조건
없음. 어휘집 관련 항목은 임시 파일로 만든다.

#### 절차
`tests/test_corrections.py`. 노이즈 판정, 고정 표기 교정(계통 오류 포함), 규칙과 글자가
겹치는 실제 메뉴가 깨지지 않는지, 빈도가 비슷한 표기 변형을 건드리지 않는지, 어휘집 교정의
길이·후보 수·최소 등장 횟수 조건, 어휘집 파일 부재·손상·예전 형식 처리, 교정 후 중복 제거와
순서 유지를 표로 나열해 고정한다.

#### Expected Result
의도한 항목만 바뀌고 나머지는 그대로이며, 어휘집이 없거나 깨져도 오류가 나지 않는다.

### TEST-STORE-001

#### 검증 대상
REQ-STORE-001

#### 선행 조건
없음.

#### 절차
`tests/test_query.py`의 주차 필터 테스트로 지난 주·다음 주 항목이 걸러지는지 확인하고,
`tests/test_cli.py`로 학습용 DB 경로가 어떤 운영 DB 경로에 대해서도 같은 파일이 되지 않는지
확인한다.

#### Expected Result
대상 주차 월~일 항목만 남고 제외 개수가 맞으며, 학습용 경로가 운영 경로와 항상 다르다.

### TEST-QUERY-001

#### 검증 대상
REQ-QUERY-001

#### 선행 조건
기준 시각을 고정한다.

#### 절차
`tests/test_query.py`, `tests/test_query_routing.py`. 요일+끼니, 끼니 단독, 요일 단독,
줄임말, 코너 이름 단독(간편식·건강식) 입력을 표로 나열해 해석 결과를 확인한다.

#### Expected Result
각 발화가 기대한 날짜와 끼니로 해석되고, 코너 이름만 말한 질문은 그 코너가 나오는 끼니만
보여준다.

### TEST-QUERY-002

#### 검증 대상
REQ-QUERY-002

#### 선행 조건
메뉴 데이터가 들어 있는 임시 DB.

#### 절차
`tests/test_query.py`, `tests/test_query_routing.py`. 지난 주·다음 주 요청, 모르는 낱말이
섞인 질문, 코너 중복·코너와 끼니 충돌, 날짜·요일·끼니 중복, 잘못된 날짜, 요일이 빠진
`이번주 …`, 식단 질문이 아닌 잡담을 표로 나열한다.

#### Expected Result
각 입력에 기대한 안내 문구가 돌아가고 메뉴가 노출되지 않는다. 모르는 낱말은 조사가 붙은
채로 잘리지 않고 온전히 인용된다. 지원하는 표현에는 되묻기가 발생하지 않는다.

### TEST-QUERY-003

#### 검증 대상
REQ-QUERY-003

#### 선행 조건
메뉴 데이터가 들어 있는 임시 DB와 고정된 기준 시각.

#### 절차
`tests/test_query.py`, `tests/test_query_routing.py`. 현재 주 밖 날짜 요청, 주말 요청,
데이터가 없는 날짜 요청, 코너별 세로 목록 응답 형식을 확인한다.

#### Expected Result
현재 주 밖 데이터는 어떤 경로로도 노출되지 않고, 주말과 미게시 상태는 실제 날짜가 포함된
안내로 답하며, 응답 본문이 끼니 → 코너 → 항목 구조를 유지한다.

### TEST-API-001

#### 검증 대상
REQ-API-001

#### 선행 조건
없음.

#### 절차
`tests/test_query.py`, `tests/test_query_routing.py`. 하루 전체 응답을 카카오 응답으로
변환해 말풍선 분할을 확인하고, 사용방법 문구가 안내 접두사가 붙은 경우까지 말풍선 하나에
들어가는지 확인한다.

#### Expected Result
끼니별로 말풍선이 나뉘고 개수 상한을 넘지 않으며, 사용방법은 한 말풍선에 들어간다.

### TEST-WATCH-001

#### 검증 대상
REQ-WATCH-001

#### 선행 조건
임시 DB·상태 파일과 가짜 수집기·메일 발송기.

#### 절차
`tests/test_next_week_watch.py`. 게시글 발견 후 확보·알림, 같은 주차 알림 중복 방지,
게시글은 있으나 항목 0건, 게시글 미발견, 수집기 예외, 이미 확보된 주차의 접속 생략,
주차가 바뀐 뒤 재확인, 상태 파일 손상을 각각 확인한다.

#### Expected Result
확보 판정이 저장된 항목 수에 따라 갈리고, 시도 기록에 결과 구분과 사유가 남으며, 이미
확보된 주차에서는 그룹웨어에 접속하지 않는다.

### TEST-WATCH-002

#### 검증 대상
REQ-WATCH-002

#### 선행 조건
임시 DB·상태 파일과 가짜 메일 발송기.

#### 절차
`tests/test_next_week_watch.py`. 마감 시점 미확보 알림, 실패 사유 요약 포함, 확보된 주차의
무알림, 같은 주차 1회 발송, 발송 실패 시 재시도, 자정을 넘겨도 대상 주차가 흔들리지 않는지,
DB 기준 확보 판정, 항목 0건에서 확보로 넘어가지 않는지, 메일 미설정 시 발송 생략을
확인한다.

#### Expected Result
확보 상태에서는 조용하고, 미확보는 사유가 담긴 알림으로 한 번만 드러나며, 발송이 실패하면
다음 실행에서 다시 시도한다.

### TEST-PARSE-002

#### 검증 대상
REQ-PARSE-002

#### 선행 조건
없음. 그룹웨어도 OCR provider도 타지 않고 OCR 줄을 직접 만들어 파서만 검사한다
(14절이 적어 둔 "파서 쪽부터 고정" 지점이다).

#### 절차
`tests/test_parse_status.py`를 실행한다 (`.venv/bin/python -m pytest`).

#### Expected Result
대체휴무·공휴일·전사휴무는 그날 세 끼 모두에 미운영으로 들어가고, `조식 미제공`은 조식에만
들어간다. 영양사 픽 같은 문구는 미운영이 아니라 특식이다. 평범한 식단은 `normal`이다
(마지막 단언이 없으면 분류기가 무엇이든 미운영으로 답해도 앞 검사들이 통과한다).

### TEST-SCRAPE-001

#### 검증 대상
REQ-SCRAPE-001

#### 선행 조건
사내망 접속과 그룹웨어 계정. 자동화할 수 없다 — 외부 시스템의 로그인과 게시판 구조에
의존하고, 그 구조는 이쪽에서 고정할 수 없다.

#### 절차
1. `.venv/bin/menu-bot collect`를 실행한다.
2. 받은 이미지 수와 게시물 제목을 출력에서 확인한다.
3. 같은 주차로 한 번 더 실행해 게시물 단위로 교체되는지 본다.

#### Expected Result
현재 주 게시물을 찾아 이미지를 모두 내려받고, 재실행이 중복 항목을 만들지 않는다.

### TEST-OCR-001

#### 검증 대상
REQ-OCR-001, NFR-PORT-001(provider 비교 부분)

#### 선행 조건
플랫폼별 OCR provider가 설치돼 있어야 한다(macOS Apple Vision, Windows·Linux PaddleOCR).
두 플랫폼이 모두 필요하므로 자동화하지 않는다.

#### 절차
같은 이미지를 두 provider로 처리하고 `<이미지>.ocr.json` 캐시를 비교한다.

#### Expected Result
좌표 규약(정규화 x·y·width·height)이 같고, 셀 구성과 휴무·특식 판정이 일치한다.

### TEST-OPS-001

#### 검증 대상
NFR-SEC-001, NFR-OPS-001, NFR-PORT-001

#### 선행 조건
없음. 자격 증명도 network도 쓰지 않는다. 이 절이 덮는 것은 세 요구사항의 '측정' 중 기계가
답할 수 있는 부분뿐이다 — 상시 유닛 강제 종료 후 자동 복구와 두 provider의 실제 이미지
비교는 각각 아래 수동 절차와 TEST-OCR-001이 맡는다.

#### 절차
`tests/test_operational_contracts.py`를 실행한다 (`.venv/bin/python -m pytest`). 이어서 운영
서버에서 `systemctl kill menubot-web` 후 유닛이 스스로 올라오는지 확인한다(수동).

#### Expected Result
`.env`·DB·원본 이미지·어휘집이 추적되지 않고 git이 실제로 무시한다. 웹훅 토큰 불일치는
404다(설정이 없는 환경에서는 소스 수준 단언이 대신 고정하며, 건너뛴 경우 그 사실이 출력에
남는다). 상시 유닛 둘은 서로 다른 파일이고 각각 `Restart=always`이며, 수집·다음주 확인
유닛에는 메모리 상한이 있고, 타이머마다 짝이 되는 서비스가 있다. 시간대는 설정값에서 오고
파서·조회에 시간대 없는 `datetime.now()`가 없다.

## 12. 요구사항 추적성

| Requirement | Implementation | Test | Status |
|---|---|---|---|
| REQ-SCRAPE-001 | `src/menu_bot/scraper.py` | TEST-SCRAPE-001 (수동) | implemented |
| REQ-OCR-001 | `src/menu_bot/ocr_provider.py` | TEST-OCR-001 (수동) | implemented |
| REQ-PARSE-001 | `src/menu_bot/parser.py` | TEST-PARSE-001 | verified |
| REQ-PARSE-002 | `src/menu_bot/parser.py` | TEST-PARSE-002 | verified |
| REQ-CORRECT-001 | `src/menu_bot/corrections.py` | TEST-CORRECT-001 | verified |
| REQ-STORE-001 | `src/menu_bot/pipeline.py` | TEST-STORE-001 | verified |
| REQ-QUERY-001 | `src/menu_bot/query.py` | TEST-QUERY-001 | verified |
| REQ-QUERY-002 | `src/menu_bot/query.py` | TEST-QUERY-002 | verified |
| REQ-QUERY-003 | `src/menu_bot/query.py` | TEST-QUERY-003 | verified |
| REQ-API-001 | `src/menu_bot/web.py` | TEST-API-001 | verified |
| REQ-WATCH-001 | `src/menu_bot/next_week_watch.py` | TEST-WATCH-001 | verified |
| REQ-WATCH-002 | `src/menu_bot/next_week_watch.py` | TEST-WATCH-002 | verified |
| NFR-SEC-001 | `src/menu_bot/web.py`, `.gitignore` | TEST-OPS-001 | verified |
| NFR-OPS-001 | `scripts/systemd/menubot-web.service`, `scripts/systemd/menubot-tunnel.service`, `scripts/systemd/menubot-collect.service` | TEST-OPS-001 | implemented |
| NFR-PORT-001 | `src/menu_bot/ocr_provider.py`, `src/menu_bot/config.py` | TEST-OPS-001, TEST-OCR-001 (수동) | implemented |

Status 값: `draft` (사양만 있음) / `implemented` / `verified` (실제 실행까지 확인) /
`deprecated`.

## 13. 미확정 사항

- **`DEFAULT_LOCATION`의 사양이 없다.** 설정 항목과 CLI·API의 인자 전달 경로는 있지만
  응답 생성에서 실제로 쓰이지 않는다. 사업장별 응답을 하지 않기로 한 범위(2절)와 맞물려,
  이 설정을 유지할지 제거할지 확인이 필요하다.
- **공통 식단을 고르는 기준이 코드에 고정돼 있다.** 특정 사업장 이름 하나를 공통 식단으로
  보고, 없으면 사업장이 한 곳일 때만 그 결과를 쓴다. 이 이름이 `POST_PREFIXES`의 첫 항목을
  뜻하는지 고정된 값인지 문서에 없다.
- **운영 DB에서 지난 주 데이터를 언제 지우는지 정해져 있지 않다.** 저장은 게시물 단위
  교체이고 조회는 현재 주로 제한되므로 사용자에게 노출되지는 않지만, 지난 주 게시물의
  항목이 DB에 계속 남는다. 정리 주기·방법을 정할지 확인이 필요하다.
- **CLI `ask`와 카카오 API의 "아는 사업장 이름" 목록이 다르다.** API는 설정값을 넘기고
  CLI는 기본 목록을 쓴다. 두 경로가 같은 판정을 해야 하는지 확인이 필요하다.
- **서버 재부팅을 통한 자동 시작은 아직 검증되지 않았다**(`README.md`에 명시). 유닛은
  부팅 시 올라오도록 등록돼 있으나 실제 재부팅 검증이 남아 있다.

## 14. 향후 개선 후보

- 터널 무료 한도에 가까워지면 서버리스 무료 엔드포인트로 공개 경로를 옮긴다.
- Windows 운영 시 터널 에이전트 최소 버전 요건을 사내 IT 승인 절차로 해소한다.
- `REQ-PARSE-002`는 2026-09-22에 자동 테스트를 붙였다(TEST-PARSE-002). 남은 둘
  (`REQ-SCRAPE-001` · `REQ-OCR-001`)은 각각 사내망 그룹웨어와 플랫폼별 OCR provider가
  있어야 관찰되므로 수동 절차로 둔다. 자동화하려면 먼저 저장된 OCR 캐시를 저장소에 둘 수
  있는 형태(개인정보·내부 주소 제거)로 만드는 결정이 필요하다.
